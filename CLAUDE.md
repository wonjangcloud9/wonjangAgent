# CLAUDE.md — 원장(wonjang) 작업 규칙

한국어 우선 로컬 AI 에이전트(Rust). 이 파일은 모든 Claude 세션과 자율 발전 루프가 따르는 코딩 헌법이다.

**트레이드오프:** 이 지침은 속도보다 신중함으로 기운다. 사소한 작업엔 판단껏(원칙을 과의식하지 말 것).

## 안드레 카파시 AI 코딩 4원칙

출처: [forrestchang/andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills). Karpathy가 지적한 LLM 코딩 4대 실패 모드(근거 없는 가정 · 혼란 방치 · 과설계 · 무분별한 직교 변경)에 대응한다.

### 1. 코딩 전에 생각하라 — "추측하지 말고, 혼란을 숨기지 말고, 트레이드오프를 드러내라"
- 가정은 명시한다. 불확실하면 추측 대신 **묻는다**.
- 해석이 여럿이면 하나를 몰래 고르지 말고 **제시**한다.
- 더 단순한 길이 있으면 **밀어붙인다(push back)**. 불일치는 드러낸다.

### 2. 단순함 우선 — "문제를 푸는 최소 코드. 추측성 금지"
- 요청하지 않은 기능·추상화·유연성·설정 추가 **금지**.
- 단발성 코드에 추상화 만들지 말 것. 일어날 수 없는 상황 방어 코드 금지.
- 시험: **시니어가 보면 과하다 할까? 그렇다면 줄여라.** (1000줄 대신 100줄)

### 3. 외과적 변경 — "꼭 필요한 것만 건드리고, 네가 만든 것만 치워라"
- 인접 코드·주석·포맷을 '개선'하지 말 것. 동작하는 컴포넌트 리팩터 금지. 기존 스타일에 맞춰라.
- 내 변경이 고아로 만든 import·변수·함수만 제거. **기존 dead code는 언급만 하고 삭제는 요청받았을 때만**.
- 시험: **바뀐 모든 줄이 사용자 요청으로 직접 추적되는가?**

### 4. 목표 주도 실행 — "성공 기준을 정하고, 검증될 때까지 돌려라"
- 명령을 **검증 가능한 목표**로 바꾼다. 다단계 작업은 각 단계와 검증 체크포인트를 먼저 그린다.
- "LLM은 구체적 목표를 향해 반복하는 데 탁월하다 — 무엇을 하라가 아니라 성공 기준을 주고 돌려라."(Karpathy)

## wonjang 고정 제약 (위 원칙과 늘 함께 적용)

- **빌드 정책**: 순수 Rust + rustls(ring provider). openssl 금지(musl 정적 빌드). 새 무거운 의존성 금지.
- **시크릿**: 환경변수로만 읽는다. 설정 파일·코드에 저장 금지.
- **검증(Verify-or-don't-ship)**: 모든 수정은 빌드한 바이너리로 **실측**한다(에러 본문·출력까지 확인). 상태를 바꾸는 명령은 `HOME=/tmp/...` 격리로 테스트하고, 실제 data_dir에서 라이브 테스트 금지. 검증 못 하는 외부 API에 의존하는 기능은 **싣지 않는다**.
- **패딩 금지**: GPT가 못 하는 진짜 가치만 추가한다. 가치가 없으면 억지 릴리스 말고 정직하게 대기한다.
- **매력 우선**(2026-06-05~): 사람이 실제로 쓰고 싶고 자랑·공유하고 싶게. 엣지 폴리시보다 매력·전염성.
- **한국어 우선 출력**: 사용자 표면에 영문 날짜·내부 오류 잔재 금지.

## 릴리스 게이트 (푸시 전 필수)

```
cargo fmt --all --check
cargo clippy --all-targets -- -D warnings
cargo test --all
```
통과 후 한 사이클 = 진짜 개선 1개 → 버전 올림 → `vX.Y.Z` 태그 푸시(GitHub Actions가 npm 자동 발행) → CI green 확인.
