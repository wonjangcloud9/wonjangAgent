# 변경 이력 (CHANGELOG)

원장 에이전트의 릴리스 기록입니다. 버전 형식은 [Semantic Versioning](https://semver.org/lang/ko/)을 따르며, npm 패키지([`wonjang-agent`](https://www.npmjs.com/package/wonjang-agent))와 GitHub Release가 동일한 태그(`vX.Y.Z`)로 발행됩니다.

## 매력·전염성

- **1.54.0** — **⏳ 소멸시효 완성일 — 법률 라인 확장** — 받을 돈(채권)의 소멸시효가 언제 끝나는지 종류별로: `소멸시효 2020-01-01 상사` → 5년(상법 64조) → 완성일 2025-01-01 + D-day. **GPT가 자주 틀리는 "어떤 채권이 3·5·10년"**(민사 10년·상사 5년·임금 3년·물품/이자 3년·음식 1년·판결 10년)을 근거 조문과 함께 정확히, 발생일+기간 날짜계산(2/29 클램프). 남았으면 D-day(중단 가능), 지났으면 "시효 완성" 경고. `limitation` 순수 모듈 + 단위 테스트 2(기간·만료일) + 로컬 라우팅 테스트. 채권 분류·시효 중단은 사안마다 달라 변호사 확인 정직 표기. 도움·팁·README·사이트 반영, 413 통과
- **1.53.0** — **👪 법정 상속분 — 전문 법률 라인 시작** — 상속재산을 누가 얼마나 받는지 민법(1009조) 기준으로: `상속 7억 --배우자 --자녀 2` → 배우자 3/7(3억)·자녀 각 2/7(2억). **GPT가 자주 틀리는 배우자 5할 가산(1.5배)·순위(1순위 자녀 우선)·동순위 균분**을 정확히. 배우자+자녀/배우자+부모/자녀만/배우자 단독 등 구성별 처리, 자녀 있으면 부모 제외 안내, 절사 나머지는 첫 상속인에게(합계 정확). `inheritance` 순수 모듈 + 단위 테스트 3(검산: 7억→3억/2억/2억) + 로컬 라우팅 테스트. 유언·유류분·기여분·상속포기 미반영 정직 표기(변호사 상담 안내). 도움·팁·README·사이트 반영, 411 통과
- **1.52.0** — **💸 원천징수 3.3% — 프리랜서·개인사업자 매일 쓰는 것** — `원천징수 100만` → 소득세 3% + 지방세 0.3% = 33,000 공제 → 실수령 967,000. **역산이 핵심:** `원천징수 967000 --역산` → 세전 ≈ 1,000,000 — "실수령×1.033"이라는 흔한 오답을 바로잡는다(정답은 ÷0.967). 기타소득(강연료·원고료)은 `--기타`로 8.8%. 세율 고정·정수 연산이라 검증 가능(검산: 100만→96.7만·기타→91.2만·역산 96.7만→100만). `withholding` 순수 모듈 + 단위 테스트 3 + 로컬 라우팅 테스트, 도움·팁·README·사이트 반영, 408 통과
- **1.51.0** — **🛒 최저가 가격비교 런처** — 실사용자 요청(상품 최저가 검색). 상품 하나로 **네이버쇼핑(가격비교)·다나와·쿠팡·구글쇼핑** 검색을 한 번에: `최저가 에어팟 프로` → 4개 비교 사이트 링크, `--열기`로 브라우저에서 모두 열기. 여러 쇼핑몰을 일일이 열고 검색어 치는 수고를 한 명령으로. 한글 검색어 자동 퍼센트 인코딩(다중 단어 OK). **정직성:** 실시간 가격은 각 쇼핑몰 API 키/스크래핑이 필요해 검증 불가(원칙 ②)라 가져오지 않고, **검증 가능한 검색 링크만**(URL 유효성 실측 — 404 아님). `pricesearch` 순수 모듈 + 단위 테스트 2(인코딩·템플릿) + 로컬 라우팅 테스트, 도움·팁·README·사이트 반영, 405 통과. (다음: 부동산·중고차·호텔 런처 — 같은 패턴)
- **1.50.0** — **🎁 증여세 추정 — 전문 세무 라인 확장** — 자녀·배우자 증여 시 세금을 증여재산공제 + 누진세율 + 신고세액공제(3%)까지 반영해 추정. `증여세 5억 자녀` → 공제(성년 자녀 5천만) → 과세표준 4.5억 → 누진세율(20%, 누진공제 1천만) → 산출 8천만 → 신고공제 3% → **납부 7,760만**. 관계별 공제(배우자 6억·미성년 2천만·직계존속 5천만·기타 1천만), 공제 범위 안이면 "낼 증여세 없음" 안내. **GPT가 자주 틀리는 공제·누진공제**를 정수 연산으로 정확히(검산: 1억→485만·5억→7,760만·6억 배우자→0·경계 연속성). 10년 합산·세대생략 할증 미반영 정직 표기. `gifttax` 순수 모듈 + 단위 테스트 3 + 로컬 라우팅 테스트, 도움·팁·README·사이트 반영, 403 통과
- **1.49.0** — **🧾 종합소득세 추정(개인사업자·프리랜서) — 전문 세무 라인 시작** — 실사용자(개인사업자) 요청으로 한국 사업자에게 유리한 전문 도구를 추가하기 시작. 5월 종소세 신고철, `종소세 5000만`(과세표준) → 누진세율 산출세액 + 지방소득세(10%) + 실효세율. **GPT가 자주 틀리는 누진공제**를 정수 연산으로 정확히(검산: 과표 5천만→624만, 1억→1956만, 경계 연속성 확인). 과세표준 기준 추정임을 명시(세액공제·감면 미반영, 2025 현행 세율 — 정확한 신고는 홈택스/세무사). `incometax` 순수 모듈 + 단위 테스트 4 + 로컬 라우팅 테스트, 도움·팁·README·사이트 반영, 400 통과. 더불어 **README 배지 추가**(npm 다운로드 수·GitHub 스타 수·라이선스 — open-guardrail 스타일). (다음 세무/법률 후보: 원천징수 3.3%·부가세 신고·증여세·법정 상속분·소멸시효 — 모두 검증 가능·안정적인 것만)
- **1.48.3** — **🐛 한글 입력 백스페이스 글자 단위로** (실사용자 신고) — 대화형에서 한글 1글자를 지우려면 백스페이스를 **3번**(=UTF-8 3바이트) 눌러야 하던 치명적 한글 UX 결함 수정. 원인: 터미널 라인 디서플린에 `IUTF8` 플래그가 꺼져 있으면(로케일 미설정 등) canonical 모드 백스페이스가 바이트 단위로 동작. 이제 대화형 진입 시 stdin TTY에 **IUTF8을 켜서** 백스페이스가 글자 단위로(한글 1자=1번). canonical 모드는 유지, 파이프(비TTY)는 손대지 않음, unix 전용. **PTY 실측 검증**(IUTF8 OFF로 버그 재현 → wonjang 실행 후 ON 확인). `libc`(이미 의존성 트리에 있던 표준 경량 크레이트) 직접 의존성화, 396 통과
- **1.48.2** — **감사 후속: 미지 슬래시 명령의 `/` 누수 제거** — 백엔드(LLM) 연결 상태에서 알 수 없는 슬래시 입력(`/foobar`)이 선행 `/`째 모델에 전달되던 것을, 슬래시·로컬 명령이 아니면 `/`를 떼고 보내도록 수정(평문은 그대로). 1.48.1 감사가 확정한 7건 중 수정 가능한 마지막 항목 — 이로써 자가 감사 확정 버그는 전부 처리(남은 1건은 clap `--help` 영문 헤더로, 라이브러리 한계라 `wonjang 도움`이 한국어 대안). 396 통과
- **1.48.1** — **품질 감사 수정 5건** — ultracode 자가 감사(4차원 병렬 리뷰 + 각 지적 적대적 검증)가 확정한 genuine 버그를 수정. ⓐ**(HIGH 회귀)** clap 사용법 줄이 *존재하지 않는 명령*을 안내하던 버그: `koreanize_command_names`가 전역 평면맵이라 별칭 없는 명령에 다른 enum의 별칭이 새어들어, `cron add` 인자 누락 시 `wonjang cron 추가`(무효)를 안내했다 → **경로 인식 트리 추적**으로 고쳐 `wonjang cron add`(유효) 안내. ⓑ**(말투 충돌)** 커스텀 '나만의' 반말 원장인데 ack·배너 인사·자랑 카드 코멘트가 존댓말로 어긋나던 것 → `soul::tone_key`로 말투 정렬(얼굴 🌟은 유지). ⓒ**(REPL 경로)** `/성격`이 picker/만들기를 못 열고 `/성격 만들기`가 에러나던 것 → `cmd_soul`로 일원화. ⓓ 사업자번호 `000-00-00000`이 체크섬상 유효로 뜨던 것 → 무효. ⓔ 첫 실행 picker가 범위 밖·이름 입력을 조용히 기본으로 확정하던 것 → 재프롬프트 + 이름('친구'…) 입력 허용. 단위 테스트(koreanize 회귀·bizno·build_custom_persona) + 격리 HOME 실측, 396 통과
- **1.48.0** — **✏️ 나만의 원장 만들기 — 커스텀 캐릭터** — 캐릭터 3탄(로드맵 완결). 프리셋 5종이 아쉬우면 직접 만든다: `wonjang 성격 만들기` → 한 줄 묘사("나를 '대장'이라 부르고 츤데레처럼 챙기는 비서") + 말투(존댓말/반말) → 나만의 원장(🌟)이 완성돼 SOUL.md에 저장. 첫 실행 선택 화면에도 `6) ✏️ 직접 만들기` 추가. ChatGPT/Claude의 "캐릭터 만들기"를 키·LLM 없이 결정론적으로(`soul::build_custom_persona`) — 누구나 자기 비서를 가질 수 있다. `build_custom_persona`·`save_persona` 순수 함수 + 단위 테스트(묘사·말투 반영), 격리 HOME 실측, 397 통과. README·사이트 동기화
- **1.47.0** — **🎭 고른 성격이 모든 명령에 — "원장이 곁에 있다"** — 캐릭터 2탄(실사용자 1순위). 1.46.0이 성격을 *고르게* 했다면, 이제 대화형에서 **명령을 실행할 때마다 원장이 자기 얼굴·말투로 한마디** 한다 — `당신 ▸ 연봉 3600` → `😎 원장 ▸ 오케이, 바로 해줄게 👌` → 결과. 성격별로 다른 반응(친구·집사·선배·발랄·기본), 입력에 따라 결정론적으로 다양하게(`soul::ack`). REPL의 명령 실행 한 지점에 입혀 **모든 로컬 명령에 자동 적용**(키 없이 쓰는 zero-setup 사용자는 전부, 백엔드 사용자는 슬래시 명령에). 평문→LLM 라우팅은 그대로(회귀 0). `soul::ack` 순수 함수 + 단위 테스트(결정론·성격별 상이), 격리 HOME 실측, 396 통과. README·사이트 동기화. (다음: 커스텀 캐릭터 만들기)
- **1.46.0** — **🎭 첫 실행 캐릭터 선택 — "내 원장 정하기"** — 실사용자 피드백("터미널 안에서 AI 비서로 있는 느낌이 안 든다, 캐릭터를 넣자")에서 출발. 페르소나는 있었지만 LLM 응답에만 묻어났고, 사용자가 *자기 비서를 고르는 순간*이 없었다. 이제 처음 실행하면 **성격부터 고른다** — 5종(다정/친구/집사/선배/발랄)이 각자 얼굴(🌙😎🎩😏✨)과 목소리로 미리 인사하고, 고르면 그 원장이 터미널에 등장한다. `wonjang 성격`(인자 없이)으로 언제든 다시 고를 수 있고, 한 번 고르면 다시 묻지 않는다(`.persona_chosen` 마커). 비대화형(파이프)에선 건너뛰어 스크립트·회귀에 영향 0. `soul::face`·`voice_sample`·`resolve_choice`·`is_chosen` + 단위 테스트(선택 파싱·프리셋별 고유 목소리), 격리 HOME 실측, 395 통과. (다음: 고른 성격을 모든 출력에 입히기 → 커스텀 캐릭터 만들기)
- **1.45.3** — **집중 시간 입력 검증**(실사용자 설치로 잡힌 플래그십 버그) — 첫 실사용자가 로컬 설치 후 `자랑` 카드를 띄우니 `🍅 이번 달 집중 3333333333333333시간 45분`처럼 깨져 나왔다. 원인: `집중` 명령에 상한이 없어 말도 안 되는 분(테스트 오염값 2×10¹⁷분)이 그대로 누적·합산돼 카드를 깨뜨렸다. 실사용자가 오타로 큰 수를 넣어도 같은 일이 난다. 이제 집중 세션은 **1분~24시간(1440분)** 으로 검증(`집중 시간이 너무 길어요(최대 24시간=1440분)`), 합성 테스트가 못 잡는 *실데이터 첫인상* 결함을 제거. `focus::check_minutes` 순수 함수 + 단위 테스트(경계 1440 허용·1441/0/음수/2×10¹⁷ 거부), 격리 HOME 실측, 393 통과
- **1.45.2** — **사용법 줄의 명령명도 한국어로**(1.45.1 후속 완결) — 1.45.1이 인자 에러 문장은 한국어로 바꿨지만, 함께 뜨는 `사용법:` 줄은 clap의 *영문 정규 명령명*을 노출했다: `나이`를 쳤는데 `사용법: wonjang age <BIRTH>`, `대출`인데 `wonjang loan …`, `디데이 추가`인데 `wonjang dday add …` — 사용자가 친 적 없는 영어 명령이라 placeholder보다 더 혼란스러웠다. 이제 정규 명령명을 **첫 한국어 별칭으로 자동 치환**(`wonjang 나이 <BIRTH>`·`wonjang 대출 <MANWON> <RATE> <MONTHS>`·`wonjang 디데이 추가 <LABEL> <DATE>`, 중첩 명령도). 별칭 맵을 명령 트리에서 만들어 **전 명령에 자동 적용**(개별 수정 0). 형식 오류의 placeholder 잔재(`(--폭 <WIDTH>)`)도 `(--폭)`으로 정리. placeholder `<BIRTH>` 자체는 필드명이라 유지(향후 value_name 과제). `koreanize_command_names` + 단위 테스트(age→나이·dday add→디데이 추가·이미 한국어면 유지), 392 통과
- **1.45.1** — **인자 에러를 한국어로**(한국어 우선 출력 완결) — 첫 사용자 첫인상 누수 제거: `wonjang 전역`처럼 필수값을 빠뜨리면 그동안 clap 기본 영문 에러(`error: the following required arguments were not provided`)가 그대로 떴다 — 제 검증 메시지(군별·날짜)는 한국어인데 clap 기본 에러만 영어라 "어 영어네" 첫인상 킬러였다. 이제 `오류: 필요한 값이 빠졌어요 / 사용법: … / 전체 기능은 → wonjang 도움`으로. 필수값 누락·모르는 옵션(`모르는 옵션이에요 (--xyz)`)·형식 오류·값 개수 등 종류별 한국어. `--help`·`--version`은 그대로 통과(정상 정보), 알 수 없는 입력은 여전히 AI 프롬프트로(trailing_var_arg라 회귀 0). 한 번의 래퍼로 **~150개 전 명령**에 적용. `clap_error_headline` 순수 함수 + 실제 clap 에러로 검증하는 단위 테스트, 391 통과
- **1.45.0** — **`전역` 전역일 계산기 + 자랑 카드** — 한국 남성·가족이 가장 집착하는 D-day를 한 줄로: `전역 2025-03-04 육군` → `전역까지 D-89 · 전역일 2026-09-03(목) · 진행률 84% · 현재 계급 병장 · 다음 진급 D-…`. `--카드`면 카톡에서 안 깨지는 **전역 자랑 카드**(군인 본인·여친·부모가 캡처·공유). 군별(육·해·공·해병·사회복무) 현행 복무기간(18·20·21개월)과 계급 진급 경계(입대 후 2·8·14개월=군인사법 최저복무기간)로 **순수 날짜 계산** — GPT가 복무기간을 옛값(21·24개월)으로 자주 틀리는 데 비해 결정론적이고 정확. 입대 전(`입대 D-`)·전역 당일·전역 후(`D+`)·사회복무(소집해제·계급 없음)까지 상태별 처리. *만기 전역일 기준(조기전역·휴가·공휴일 미반영)* 정직 표기. 알려진 전역일(2022-01-03 육군→2023-07-02)로 검산, 카드 폭 불변식 통과. `military` 순수 모듈 + 단위 테스트 6 + 로컬 라우팅 테스트 + 도움·팁·README, 390 통과
- **1.44.0** — **`사업자번호` 검증** — 세금계산서·계약서 오타를 잡아요: 자영업자·프리랜서·회계가 거래처 사업자등록번호를 입력할 때 오타·허위를 거르는 국세청 검증번호(체크섬). `사업자번호 124-81-00998` → `✅ 유효`, 끝자리 오타면 `❌ 무효`. 가중치 합(1,3,7…) 알고리즘이라 **GPT가 자주 틀리는데 결정론적**이고, 사업자번호는 공개정보라 민감하지 않다(주민번호와 다름). 형식·체크섬만 보고 *실제 사업자 존재는 국세청 조회 필요*임을 정직하게 안내. 공개 번호(삼성·카카오)로 검산, 하이픈 유무 무관. `bizno` 순수 모듈 + 단위 테스트 + 도움·팁·README, 384 통과
- **1.43.2** — `색` 도움말에 **`rgb()` 형식 안내** — 1.43.1 기능 발견성 완결: rgb() 입력을 받게 됐는데 도움말은 `#ff5733·255 87 51`만 보여줘 사용자가 새 형식을 몰랐다. 이제 `예: 색 #ff5733 · 255 87 51 · "rgb(255,87,51)"`로 노출(모듈 doc도). 만든 기능이 실제로 쓰이게 하는 마무리, 382 통과
- **1.43.1** — `색`이 **`rgb(255,87,51)` 복붙을 받아요** — 양방향 변환 완성: `색`이 RGB를 *출력*하면서 정작 CSS·디자인툴에서 복붙하는 `rgb(255,87,51)`·`255,87,51`(콤마) *입력*은 거부해(헥스·공백3개만), 한 방향뿐이었다. 이제 `색 "rgb(255,87,51)"`·`색 255,87,51`도 → `#FF5733`. CSS·Figma에서 그대로 복붙. 기존 헥스·`색 255 87 51`은 그대로(회귀 0). `color::parse_rgb` 순수 함수(rgb()·콤마·공백·범위검사) + 단위 테스트 5형식, 382 통과
- **1.43.0** — **`연차수당`** — 안 쓴 연차를 돈으로: 기존 `연차`는 일수만 보여줘 "안 쓴 연차 수당으로 얼마?"(퇴사·연말 정산의 흔한 질문)엔 답을 못 했다. 이제 `연차수당 300 5`(월급 300만·미사용 5일) → `1일 통상임금 114,833원 · 연차수당 574,163원`. 근로기준법 통상임금 기준(월급÷209×8×일수), 반차(0.5)도 OK. `연차` 출력에서 `연차수당`으로 크로스링크(일수 → 돈 자연스럽게). 통상임금=월급 가정이라 정직하게 '추정' 표시. `annual_leave::unused_leave_pay` 순수 함수 + 단위 테스트(검산 574,163), 별칭 `미사용연차·연차정산`, 도움·README 노출, 381 통과
- **1.42.3** — 디데이 추가 확인 메시지에 **요일 표시**(목록·카드와 일관) — 등록 즉시 '무슨 요일?'을: 디데이를 추가하면 확인 메시지가 `(2026-11-19, D-166)`로 날짜만 보여줘, 목록·카드(요일 표시)와 달랐다. 이제 `(2026-11-19 목, D-166)`로 — 시험·전역·발표가 무슨 요일인지는 계획에 직결되고(다시 목록 안 봐도 됨), 모든 디데이 화면이 요일로 일관. 검증한 표면(칼로리 TDEE·세계시간 DST)은 모두 정확함을 확인한 사이클의 마무리 일관성 보강, 380 통과
- **1.42.2** — 기념일 카드의 **연 단위 마디를 'N주년'으로** — 커플이 쓰는 말로: 기념일 카드가 `다음 1460일까지 D-358`처럼 raw 숫자만 보여줘, 커플이 챙기는 **'4주년'**임을 한눈에 못 알아봤다. 이제 365 배수 마디는 `다음 1460일(4주년)까지`·`다음 365일(1주년)까지`로 — 백일·천일 같은 100일 단위 마디는 그대로(그건 일수로 챙기니까). 공유 카드(커플이 캡처)가 더 와닿게. 실측(주년·비주년), 380 통과
- **1.42.1** — 매년 디데이가 **캘린더에서도 매년 반복** — 1.42.0의 마지막 조각: `디데이 내보내기`(.ics)가 매년 디데이를 **일회성 과거 일정**으로 내보내(결혼기념일 2020-05-20을 2020년에만) 구글·애플 캘린더에 import하면 쓸모없었다. 이제 매년 디데이엔 `RRULE:FREQ=YEARLY`를 붙여 **캘린더에서 매년 자동 반복**(일회성은 그대로). 표준 iCalendar 방식이라 모든 캘린더 앱이 인식. 단위 테스트(RRULE 유무·이벤트 수) + 실측, 380 통과
- **1.42.0** — **매년 반복 디데이**(`--매년`) — 생신·결혼기념일이 매년 자동으로: 디데이는 일회성이라 지나면 '지남'으로 끝나 **결혼기념일·부모님 생신**을 매년 재등록해야 했다. 이제 `디데이 추가 "결혼기념일" 2020-05-20 --매년` → 지나도 **다음 발생일로 굴러간다**(올해 5/20 지났으면 내년 5/20, D-348). 모든 화면(추가 확인·목록·D-day 카드·현황·자랑 카드)이 다음 발생일로 **일관 표시**하고 `🔁`로 반복임을 알린다. 2/29는 평년이면 2/28로 클램프. 월급날(월 반복)에 이은 연 단위 반복 — 한국인이 챙기는 반복 기념일을 한 번 등록으로. `effective_date` 단일 헬퍼로 8+ 호출처를 라우팅, 백워드 호환(기존 디데이 불변), 단위 테스트(다음 발생·2/29·비반복) + **5개 화면 전수 실측**(불일치 0), 380 통과
- **1.41.3** — 습관 **완료 취소**(실수로 체크한 것 되돌리기) — 로그 정직성: 잘못 누르거나 "사실 안 했는데"를 고칠 방법이 습관 전체 삭제뿐이라, **거짓 체크가 streak을 부풀렸다**. 이제 `습관 취소 운동`(어제·날짜도)으로 그 날 완료만 지운다 → `'운동' 오늘 완료를 취소했어요`. 없는 기록을 취소하면 안내만(무해), 미래는 거부, 다른 날 기록은 보존. 1.41.2 백필과 짝을 이뤄 **습관 로그 편집 3종 완성**(완료·메우기·취소). `uncheck_on` 메서드 + 단위 테스트(retain·streak) + 실측, 380 통과
- **1.41.2** — 습관 **깜빡한 날 메우기**(백필) — 억울하게 끊긴 streak 복구: `습관 완료`가 오늘만 찍혀, **어제 운동했는데 체크를 깜빡하면** streak이 끊겨 동기부여·자랑 카드가 망가졌다(습관 트래커의 흔한 마찰). 이제 `습관 완료 운동 어제`(또는 `그제`·`2026-06-05`)로 지난 날을 메우면 연속이 **이어져 복구**(예: 끊겨 1일 → 메우니 3일). 미래 날짜는 거부(아직 안 한 날). 메움이면 `'운동' 6월 5일 체크 완료(메움) — 🔥 3일 연속`으로 명확히. `check_on(날짜)` + `parse_habit_when`(어제·그제·YYYY-MM-DD) + 단위 테스트(streak 복구), 실측, 379 통과
- **1.41.1** — `월급날`이 **주말 입금 관행까지** 챙겨요 — 한국 급여 현실 반영: 월급날(예: 25일)이 토·일이면 한국 회사 대부분이 **앞 영업일(금)에 입금**하는데, 그러면 실제 입금일이 1~2일 빠르다. 이제 월급날이 주말이면 `💡 주말이라 보통 06-26(금)에 들어와요`로 안내(날짜는 그대로 두고 '보통'으로 — 회사마다 다를 수 있어 단정 안 함). 공휴일 겹침은 데이터가 필요해 주말만 본다. `payday::weekend_early_payday` 순수 헬퍼(토→금·일→금·평일→None) + 단위 테스트, 실측, 378 통과
- **1.41.0** — **`월급날` D-day** — 한국 직장인이 가장 사랑하는 카운트다운: 월급날은 *매달 반복*이라 일회성 `디데이`로는 안 맞아(25일 지나면 '지남'으로 끝남) 매달 다시 등록해야 했다. 이제 `월급날 25` → `💰 월급날 D-19 · 다음 월급날 2026-06-25 (목) · 화이팅! 곧 들어와요`. 지났으면 **자동으로 다음 달**, `월급날 말일`(말일 지급)·그 달에 없는 날(`31` → 30일 달이면 말일로 클램프)·윤년 2월·연말 넘김까지 처리. D-day별 응원 코멘트(오늘이면 "🎉 오늘이 월급날!"). 별칭 `급여일·월급`, 팁·도움·README 노출. `payday` 순수 모듈(파싱·롤오버·클램프) + 단위 테스트 4종, 실측, 377 통과
- **1.40.4** — 습관 통계: **요일 패턴이 동률이면 '가장 꾸준한 요일'을 단정하지 않아요** — 거짓 인사이트 방지: 요일별 완료가 `화1 수1 목1 금1 토1 일1`처럼 동률인데도 `max_by_key`가 동률 중 마지막(일)을 골라 `일요일에 가장 꾸준해요`라고 단정했다(6동률인데 일요일만 콕 — 오해). 이제 **유일한 1등일 때만** 그 줄을 보여주고, 동률이면 생략(요일별 분포는 그대로 표시되니 정보 손실 0). 명확한 1등(월 3번)은 그대로 표시. `Stats::dominant_weekday` 순수 메서드(동률→None) + 단위 테스트, 회귀 0, 373 통과
- **1.40.3** — 자랑 카드의 **'가장 긴 연속'이 진짜 최장 기록**을 보여줘요(라벨↔값 불일치·자랑거리 과소표시 수정) — 전염 중심 카드의 버그: 카드가 `🔥 가장 긴 연속 운동 N일`이라 적고는 실제론 **현재 streak**을 보여줬다(예: 5일 연속 기록이 있는데 끊겼다 재시작해 `현재 1일`이면 카드가 `가장 긴 연속 1일`로 표시 — 라벨과 모순이고 사용자 기록을 과소표시). 이제 라벨대로 **최장 연속(`stats.longest`)**을 보여주고(`가장 긴 연속 5일` = 진짜 기록 = 더 자랑할 만함), **코멘트는 정직하게 현재 streak** 기준으로 분리(기록은 자랑, 코멘트는 지금 상태). 주간 카드도 동일 수정. 실측(최장≠현재·최장=현재·신규 3종), 회귀 0, 372 통과
- **1.40.2** — 새 **나이 공유 카드를 발견 surface에 노출**(팁·도움) — 만든 기능이 묻히지 않게: 1.40.0에서 `나이 --카드`(살아온 날 공유 카드)를 추가했는데, 정작 '오늘의 팁' 회전과 `도움`에선 나이가 **텍스트 출력**만 가리켜 새 공유 카드가 발견되지 않았다(자랑·디데이·기념일 팁은 카드를 가리키는데 나이만 빠짐). 이제 팁 `내 살아온 날 카드 자랑 → wonjang 나이 1990-05-15 --카드` + 도움 `(--카드로 공유)`로, **공유 카드 4종(자랑·디데이·기념일·나이)이 발견 회전에 일관되게** 노출. 만든 전염 기능이 실제로 쓰이게 하는 마무리, 372 통과
- **1.40.1** — `공휴일`에서 **제헌절은 '쉬는 날 아님'으로 표시** — 휴가 계획 오해 방지: 공휴일 목록이 제헌절(7/17)을 다른 공휴일과 똑같이 D-day까지 보여줘, 2008년부터 휴무일이 아닌데 **쉬는 날로 착각해 휴가를 계획**할 수 있었다(연휴·황금연휴 계산은 이미 제외했지만 목록 표시는 안 했음). 이제 `07-17 (금) 제헌절 D-41 (쉬는 날 아님)`으로 명시. `is_day_off` 헬퍼를 단일 진실 원천으로 만들어 연휴 로직과 목록이 같은 규칙을 쓰게(드리프트 방지) + 단위 테스트, 실측, 372 통과
- **1.40.0** — `나이`에 **살아온 날 공유 카드**(`--카드`) — 모두가 가진 가장 자랑하기 쉬운 카드: 디데이·기념일엔 공유 카드가 있는데, **누구나 가진 "내 나이·살아온 날"**엔 없었다. 이제 `나이 1990-05-15 --카드` → `13,171일`이 주인공인 한 장(만 36세·말띠·황소자리 헤드라인 + `🎉 14,000일까지 D-829` 마디 hook + 다음 생일 D-day). "나 오늘로 13,171일 살았어!"는 연인 기념일·시험 D-day보다 **대상이 넓어** 전염되기 쉽다. 카톡폭(34)·긴 띠 조합(원숭이띠 물병자리)·오늘 생일 케이스까지 정렬 불변식 유지. 텍스트 모드엔 `공유 카드: …--카드` 발견 힌트. 검증된 `render_event_card` 재사용(새 의존성 0), 실측, 371 통과
- **1.39.6** — `json` 명령 에러도 **한국어로 일관** — 영문 OS·serde 잔재 제거: 다른 파일 명령(엑셀·해시·압축)은 없는 파일에 `파일을 찾을 수 없어요`인데 `json`만 `No such file or directory (os error 2)` 영문을 토했고, 형식 오류엔 `…: expected ident at line 1 column 2` 영문 serde 메시지가 붙었다. 이제 `파일을 찾을 수 없어요: x.json` · `JSON 형식이 올바르지 않아요 (1행 2열 근처)`로 한국어 우선 통일. 파싱 로직을 `parse_str`로 분리해 테스트 가능하게 + 단위 테스트(영문 잔재 없음 검증), 371 통과
- **1.39.5** — 네트워크 끊겼을 때 **깔끔한 한국어 에러**(영문·내부 URL 제거) — 모바일·약한 와이파이에서 흔한데 첫인상 망치던 걸: 인터넷이 끊기면 날씨·환율·지하철·코인·미세먼지가 `날씨 요청 실패: error sending request for url (https://api.open-meteo.com/v1/forecast?latitude=37.5665…)`처럼 **영문 reqwest 체인 + 내부 URL**을 그대로 토해냈다. 이제 `날씨 요청 실패 — 인터넷 연결을 확인해 주세요.`로(한국어 맥락은 보존, 영문·URL은 감춤). 최종 에러 표시 한 곳(`friendlyize_error`)에서 처리해 **모든 네트워크 명령에 일괄** 적용. 잘못된 도시·통화 같은 **비네트워크 에러는 그대로**(이미 친절한 한국어라 보존). 단위 테스트(네트워크↔일반 구분) + 무네트워크 실측 4종, 370 통과
- **1.39.4** — BMI가 **키↔몸무게 바꿔 입력을 짚어줘요** — 흔한 실수에 헛값을 정답처럼 안 보이게: `bmi 70 175`처럼 키·몸무게 순서를 바꿔 넣으면 `BMI 357.1`이 나오는데, 그게 맞는 줄 알 수 있다. 이제 BMI가 사람 범위(약 12~80)를 크게 벗어나면 `ⓘ 값이 비현실적이에요 — 순서는 '키(cm) 몸무게(kg)'예요`로 짚어준다(순서 혼동·자릿수 오타 모두). 실제 고도비만(150cm 160kg=BMI 71)은 범위 안이라 **오탐 없이** 그대로 계산. GPT는 357을 그냥 계산하지 경고 안 함 — 로컬 도구의 이점. 검산(범위 경계), 실측(바꿔입력·정상·극단체형 3종), 369 통과
- **1.39.3** — `수면`에 **취침 시각 모드** — "23시에 잘 건데 언제 일어나?"에 답: 수면 계산기가 `수면 07:00`(기상 시각→취침 추천)·`수면`(지금 자면→기상 추천) 두 모드만 있어, 밤에 가장 흔한 질문인 **"이 시각에 잘 건데 언제 일어나는 게 좋지?"**(취침 시각 지정)에는 답을 못 했다(내부 함수는 이미 있었는데 입구가 없었음). 이제 `수면 취침 23:00` → `23:00 취침 기준 — 03:44·05:14·06:44…`(90분 주기 끝). 키워드는 앞·뒤 아무 데나(`수면 23:00 취침`도 OK), `취침·자기·잘때`. 기존 `수면 07:00`(기상)은 그대로(회귀 0). 모드 파싱 순수 함수 + 단위 테스트, 실측·검산(23:14+4.5h=03:44), 369 통과
- **1.39.2** — 첫 화면이 **상황에 맞는 안내**로 — LLM 없는 신규 사용자에게 거짓 약속 제거: 처음 실행 온보딩이 백엔드 유무와 무관하게 `"무엇이든 한국어로 시키면 해드려요 (예: 오늘 서울 날씨)"`라고 안내해, **키 없이 설치한 사용자**(1.39.0의 주 타깃)가 권유받은 자연어를 쳐도 "백엔드 필요"로 막히던 모순을 고쳤다. 이제 백엔드가 없으면 **바로 되는 로컬 명령**을 권한다 — `자랑 · 지출 추가 5만 식비 · 디데이 추가 수능 2026-11-19 · 연봉 3600` + "자연어(AI)까지 쓰려면 claude 로그인". 백엔드 연결된 사용자의 안내는 그대로(회귀 0). 실측(양쪽 분기), 368 통과
- **1.39.1** — npm 페이지를 **"한국인을 위한 개인 비서"로 전면 재작성** — 첫인상=발견의 입구: npmjs.com 랜딩이 "API 키 설정"부터 시키는 38줄짜리 'AI 에이전트' 소개라, 키 없이 바로 쓰는(1.39.0) 한국 생활 비서라는 정체성을 못 보여줬다. 이제 **제로 셋업 강조 → 자랑 카드(wow) → 생활 시나리오별 기능 카탈로그(돈·시간·자기관리·한국생활·실시간·도구) → 한국식 입력(5만·2026.11.19·1시간 30분) → AI 확장(선택)** 순으로, 한국인이 "와 이거 다 되네" 느끼게 구성. 인용한 명령은 전부 실재 확인(랜딩↔실제 일치 — QR은 `qr` 소문자로 교정). package.json 설명·키워드도 가계부·디데이·비서 등 한국어 검색어로 보강(npm 노출↑)
- **1.39.0** — **LLM 없이도 실사용 가능** — 설치하면 누구나 바로 쓰는 제품으로: 그동안 Claude Code·API 키가 **없으면** `wonjang`(인자 없이) 실행 시 백엔드 에러로 **대화형 화면조차 못 보고 종료**됐다(일회성 `wonjang 자랑`만 가능). 이제 백엔드가 없어도 **대화형에 진입**해 `디데이 추가 수능 2026-11-19`·`지출 추가 5만 식비`·`자랑`·`대출 30000 4.5 360` 같은 로컬 명령을 **그대로 입력해 바로** 쓴다(자연어만 백엔드 안내). 슬래시(`/자랑`)는 백엔드 유무와 무관하게 로컬 실행하고, 온보딩 예시를 그대로 복붙(`wonjang 습관 추가 운동`)해도 동작(선행 `wonjang` 자동 제거). LLM 연결된 사용자의 평문은 그대로 에이전트로(회귀 0). → **계산기·가계부·디데이·공유 카드 전부 제로 셋업으로 사용**. 자기-재실행 서브프로세스(디스패치 리팩터 회피)·8MB 스레드 clap 파싱, 헬퍼 단위 테스트(따옴표·로컬↔자연어 구분·복붙), 368 통과, LLM 0 환경 실측
- **1.38.13** — 금융 계산기 큰 금액을 **`3억`처럼 자연스럽게**(`30000만 원`→`3억`) — 읽기 마찰 제거 + 검산: 대출·연봉·예금·적금이 큰 금액을 `30000만 원`·`12000만 원`처럼 보여줘 한국인이 머릿속으로 억 단위 환산을 해야 했다(전월세는 이미 `3억`으로 잘 보여주는데 불일치). 이제 검증된 `fmt_eok`로 `3억`·`1억 2,000만원`·`1억`처럼 억/만+콤마로 통일 — 즉시 읽힌다. (퇴직금 월급은 항상 작아 `만원` 유지) 겸사겸사 대출·전월세·부가세를 손계산과 **독립 검산**해 정확함을 확인(월상환 1,520,056원·전환월세 916,667원 일치 — 버그 0). 순수 함수 재사용, 실측, 366 통과
- **1.38.12** — 자랑 카드: **갓 시작한 사람(streak 1)에게 맞는 격려**로 — 첫 카드의 맥 빠지는 톤 교정: streak 1일(오늘 막 한 칸 채운 사람)이 streak 0(아직 안 함)과 똑같이 `오늘 한 칸부터 채워봐요`(시작도 안 한 사람용)를 받아, **첫 자랑 카드를 본 신규 사용자에게 맥 빠지던** 걸 고쳤다. 이제 streak 1엔 성격별로 `운동 오늘 채웠어요! 🌱`·`운동 오늘 채웠다! 💪`처럼 '오늘 해냈다' 격려(첫날·재시작 모두 자연스러운 톤). 전염 깔때기 입구의 감정 톤은 첫인상에 직결 — 첫 카드가 보람차야 공유로 잇는다. 폭 34에도 안 잘리게 간결화, 단위 테스트(streak1 ≠ streak0·'오늘' 포함), 366 통과
- **1.38.11** — 자연어 별칭 **전수 점검**: 흔한 단어 8종을 AI 위임 → 즉시 로컬로 — 1.38.10의 연봉·더치페이 발견을 일반화해, 한국인이 1순위로 칠 자연어들을 전수 테스트하니 **로컬 기능이 있는데 그 단어가 별칭이 아니라 AI로 위임**되는 게 무더기로 나왔다(느림·예측 불가). 메운 것: `비밀번호`→비번 계산기, `만나이`·`세는나이`·`나이계산`→나이, `잠`→수면, `단위`·`환산`→변환, `야근`→야근수당, `자소서`→글자수. 이제 가장 자연스러운 한 단어가 곧장 로컬 계산. (전세/월세는 전월세 *전환* 전용 계산이라 오인 위험으로 보류, 음력·띠·택배는 기능 미존재라 위임 유지) 기존 별칭 회귀 0, 별칭 해석 테스트 8종 + 실측, 366 통과
- **1.38.10** — `연봉`·`더치페이` 자연어가 **곧장 로컬 계산**(AI 위임 → 즉시 실행) — 가장 흔한 단어가 막히던 발견 갭: 연봉 실수령·더치페이 계산기가 별칭 `실수령`·`더치`로만 잡혀, 한국인이 가장 자연스럽게 치는 `연봉 3600`·`더치페이 50000 4`가 로컬 명령을 못 만나 **AI로 위임**(느리고 예측 불가, 미연결 시 실패)되던 걸 고쳤다. 이제 `연봉`·`더치페이`가 즉시 로컬 계산. 덤으로 더치페이 총액도 한국식 금액(`더치페이 5만 4`) 수용. 실수령 계산은 이미 검증된 모듈(2025 요율·추정 고지 명시)이라 새 stale 위험 0 — 자연어 입구만 연 것. 별칭 해석 테스트 + 실측(자연어·약어 회귀), 366 통과
- **1.38.9** — 부가세·할인도 **한국식 금액 표기 수용**(`5만`·`1억`·`50,000`) — raw-원 금액 입력 일관성 완성: 1.38.7에서 가계부가 `5만`을 받게 됐는데, 같은 raw-원 계산기인 **부가세**(`부가세 5만`)·**할인**(`할인 5만 30`)은 여전히 `invalid float literal` 영문 에러를 뱉었다. 이제 세 곳 모두 동일하게 만·억·콤마를 받아, 사용자가 "wonjang은 5만·1억을 안다"를 한 번 배우면 어디서나 통한다(일관성=매력). f64 필드용 얇은 래퍼 `expenses::parse_won_f64`(원은 정수라 i64 파싱 후 f64)로 타입·수식 불변·저위험. 만원-단위 계산기(퇴직금·연봉)는 의도적으로 제외(거기선 300=300만원이라 만/억 적용 시 오히려 틀림). 순수 숫자 회귀 0, 실측, 366 통과
- **1.38.8** — 약속·집중·타이머가 **한국식 시간 표기를 그대로 받아요**(`30분`·`1시간`·`1시간 30분`) — "30분 뒤"로 생각하는 한국어 그대로: 한국인은 시간을 분·시간으로 말하는데(`30분 뒤`·`1시간 뒤`) 약속·집중·타이머가 raw 분 숫자만 받아 `30분`·`1시간`에 `invalid digit found in string` 영문 에러를 뱉던 걸 고쳤다(매일 쓰는 알림·뽀모도로). 이제 `30`=30분·`30분`·`1시간`=60·`1시간 30분`=90·`1.5시간`=90을 모두 받고, 순수 숫자는 그대로(회귀 0), 못 알아들으면 한국어 예시 안내. `util::parse_minutes` 순수 파서 1곳을 세 명령(약속·집중·타이머)이 공유 + 단위 테스트(분·시간·복합·소수·미지원) 11종, 실측, 366 통과
- **1.38.7** — 가계부가 **한국식 금액 표기를 그대로 받아요**(`5만`·`1억`·`50,000`) — 돈을 말하는 한국어 그대로: 한국인은 돈을 만·억 단위로 말하는데(`5만원`·`1억`) 가계부 `지출 추가`가 raw 숫자만 받아 `5만`·`50,000`(콤마)에 `invalid digit found in string` 영문 에러를 뱉던 걸 고쳤다(매일 쓰는 핵심 입력). 이제 `5만`=50,000·`1억`=1억·`5만원`·`1.5억`=1.5억·`100만`·콤마·`원` 접미사를 모두 받고, 못 알아듣는 입력엔 한국어로 예시 안내. 조합(`3천만`)은 모호함 방지로 미지원(순수 숫자로 입력 가능). `expenses::parse_won` 순수 파서 + clap value_parser, 단위 테스트(만·억·천·콤마·원·범위·미지원 한글) 12종, 실측, 365 통과
- **1.38.6** — 날짜 입력이 **한국식 표기를 그대로 받아요**(`2026.11.19`·`2026/11/19`·`20261119`) — "오 편하다" 첫인상: 한국에서 가장 흔한 날짜 표기인 점(`.`) 형식조차 디데이·기념일·나이가 거부하고 `input contains invalid characters` 영문 에러를 뱉던 걸 고쳤다(매력 기능인데 날짜 입력에서 막히면 첫인상 손실). 이제 `.`·`/`·공백·8자리·formal `2026. 11. 19.`·한 자리 월일(`1990.5.15`)을 모두 표준형으로 정규화해 받고, 표시는 어디서나 `2026-11-19`로 통일(확인 메시지·목록·카드 일치). 진짜 엉뚱한 입력은 여전히 친절한 한국어 에러. `datecalc::normalize_date` 공유 정규화 함수 1곳에서 디데이·기념일·나이 3개 파서가 함께 받게(중복 0) + 단위 테스트(형식 6종·한자리 0채움·정규화 불가 보존), 실측, 364 통과
- **1.38.5** — 자랑 카드: **카톡 폭에서 D-숫자·연속일이 사라지던 버그 수정** — 공유 아티팩트의 핵심값 보존: 권장 카톡 폭(34)에서 현실적인 디데이 이름(`유럽 여행 출발`·`프로젝트 마감`·`둘째 출산 예정일`)이 오면 카드의 디데이 행이 오른쪽부터 잘려 **정작 핵심인 `D-202`가 통째로 사라지던** 치명적 결함을 고쳤다(전염의 중심인데 공유하면 디데이 숫자가 없는 카드가 됨). 이제 값이 좁은 폭에 안 들어가면 **가운데를 `…`로 접어 앞 이름과 뒤 핵심 수치를 모두 보존**(`유럽 여… D-202`). 연속일(`…일 42일`)에도 동일 적용. 짧은 이름(대다수)은 그대로. `fit_middle`(가운데 접기)·`truncate_tail_width`(꼬리 보존) 순수 헬퍼 + 단위 테스트(현실 라벨 3종 D-숫자·연속일 보존 + 전 줄 폭 불변식), 폭 30~52 회귀, 363 통과
- **1.38.4** — 한국어 우선 동사 **전 트래커로 완성 + 첫인상 줄 교정**: 지난 1.38.3에서 놓친 두 곳을 메웠다 — ① 신규 사용자가 처음 보는 **첫 실행 온보딩 줄**이 아직 `wonjang 습관 add 운동`(영문)을 가르치던 걸 `습관 추가`로(가장 눈에 띄는 첫인상 줄), ② **약속(remind)·즐겨찾기(bookmark)**도 `추가·삭제` 한국어 동사를 받도록(이제 습관·지출·할일·디데이·약속·즐겨찾기 6개 트래커 전부 한국어 동사 일관). 영문 동사도 유지해 회귀 0. 별칭 파싱 테스트에 약속·즐겨찾기 추가, 전 트래커 실측(영문 회귀 포함), 362 통과
- **1.38.3** — 핵심 트래커가 **입력 동사까지 한국어로** — 한국어 우선 마찰 제거: 가장 많이 치는 `습관·지출·할일·디데이`의 입력 동사가 영어(`add`·`done`·`remove`·`clear`)만 받아, 자연스러운 `습관 추가`를 친 한국인이 `error: unrecognized subcommand '추가'` 영문 에러를 만나던 걸 고쳤다(전염의 출발점인 습관 명령에서 특히 치명적). 이제 **추가·완료·삭제·정리·이번달**이 곧장 동작하고, 도움말·온보딩 예시·README도 전부 한국어 형으로 통일(첫인상). 영문 동사(`add`/`done`)도 그대로 유지해 회귀 0. 동사 단어는 기존 도움말 문구(추가/완료/삭제/정리)와 일치. 9종 별칭 파싱 단위 테스트 + 전 트래커 실측(영문 회귀 포함), 362 통과
- **1.38.2** — `집중`에 **이번 주·이번 달 누적** — 뽀모도로 동기부여: 그동안 오늘 집중 시간만 보여 **이번 주에 얼마나 쌓았는지**가 안 보이던 걸, `최근 7일 2시간 5분 · 이번 달 2시간 5분`으로. 누적 시간이 곧 동기부여(공부·작업 트래킹). 첫날엔 오늘과 중복이라 history가 있을 때만 표시(첫인상 깔끔). 내 데이터 추세라 GPT 못 함. `since_total`(최근 N일)·`month_total` 순수 메서드 + 단위 테스트, 양쪽 상태 실측, 361 통과. → **인사이트 3종 완성**(습관 통계·지출 예상·집중 추세)
- **1.38.1** — `지출`에 **일평균 + 월말 예상** — "이 페이스면 이번 달 얼마?" 예산 awareness: 가계부가 누적 합계만 보여 **이 페이스면 월말에 얼마 쓸지**가 안 보이던 걸, `일 평균 22,333원 · 이 페이스면 월말 약 670,000원`으로. 현 페이스 유지 가정의 추정(정직하게 "이 페이스면")이라 월초엔 변동 크지만, 월중부터 과소비 경고로 유용. 내 데이터+예측이라 GPT 못 함. `expenses::pace` 순수 함수 + 단위 테스트(일평균·월말 예상·0일 방어), 실측, 360 통과
- **1.38.0** — **습관 통계**(`습관 통계 <이름>` — 달성률·최장 연속·요일 패턴): 습관을 한동안 쌓으면 "이번까지 며칠 했지? 무슨 요일에 잘 빠지지?"가 진짜 동기부여 인사이트인데(내 데이터, GPT 못 함) streak·잔디만 보였다. 이제 `습관 통계 운동` → `달성률 18/30일 (60%) ████████░░░░ · 가장 긴 연속 12일 · 요일별 월3 화2 수4… · 수요일에 가장 꾸준해요`. 자랑 카드의 원천인 습관을 더 보람차게 보여 습관→자랑→전염 루프를 강화. `habits::stats` 순수 함수(달성률·최장 연속·요일 분포) + 단위 테스트(연속·요일·빈 기록 None), 검증된 `card::hbar` 재사용, 실측, 359 통과
- **1.37.5** — 디데이에 **요일** 표시(목표일이 무슨 요일인지 — 계획에 중요): `디데이` 목록과 `현황`이 날짜만 보여줘 "수능이 무슨 요일?"을 따로 확인해야 했다. 이제 `D-166 수능 (2026-11-19 목)`·현황은 `수능 (목)`으로 요일을 함께 — 시험·전역·발표 날 요일은 계획에 직결되고, 디데이 카드(이미 요일 표시)와도 일관. 검증된 `datecalc::weekday_kr` 재사용, 실측(수능=목·전역=월), 358 통과
- **1.37.4** — `습관` 목록에 **최근 7일 잔디** — 매일 체크의 보람이 한눈에: 습관 목록이 ✓/·와 🔥연속만 보여 **최근 진척이 안 보이던** 걸, `운동  ░░░░░░▓  🔥1일`처럼 7일 잔디(오래된→오늘)를 붙여 한 주 흐름을 즉시 보이게. 습관은 자랑 카드의 원천이라, 매일 체크가 보람차야 **습관→자랑→전염 루프**가 돈다. 이름은 표시폭 정렬(6글자까지). 검증된 `card::render_jandi`·`truncate_pad` 재사용, 실측, 358 통과
- **1.37.3** — `지출` 분류별에 **막대그래프** — "뭐에 제일 많이 썼지?"가 한눈에: 매일 보는 가계부 화면의 분류별 지출이 숫자 나열뿐이라 비중이 안 보이던 걸, `장보기 ██████████ 89,000원`처럼 **비중 막대**를 붙여 어디에 많이 쓰는지 즉시 보이게. 한글 분류명도 표시폭 기준으로 정렬(기존 글자수 기준 패딩의 미세 어긋남도 교정). 가계부는 매일 쓰는 핵심 화면이라 작아 보여도 실사용 체감이 크다. 검증된 `card::hbar`·`truncate_pad` 재사용, 실측, 358 통과
- **1.37.2** — `나이`에 **세는나이** 추가 — "우리 나이로 몇 살?"에 즉답: 만나이·연나이는 보여줬는데 일상에서 가장 많이 쓰는 **세는나이("한국 나이")**가 빠져 있었다. `나이 1990-05-15` → `만 36세 · 세는나이 37세 · 연 36세`로 한국에서 마주치는 **세 나이 체계를 한눈에**. 2023년 만나이 통일법으로 공식 폐지됐어도 일상 대화에선 여전히 쓰여 모두가 묻는데(GPT는 +1을 자주 헷갈림), 폐지 사실도 함께 안내. 기존 `나이` 명령 확장(새 명령 0). `counting_age` 순수 함수 + 단위 테스트(= 연나이+1), 358 통과
- **1.37.1** — README(랜딩 페이지) 최신 기능 반영 — 전염 깔때기 입구 누수 차단: 카드 풋터 `→ wonjang-agent`를 보고 npm·GitHub에 온 사람이 보는 랜딩 페이지가, 36개 릴리스 동안 추가한 고가치 기능을 **안 보여줘 wonjang을 저평가**시키던 걸 수정. **공유 카드 3종**(자랑·**D-day**·**기념일**)을 히어로에 명시하고, 계산기 표에 **전월세·퇴직금·연차·야근수당·자동차세·로마자·자소서 글자수·전통단위**를, 실시간 정보에 **황금연휴 연차꿀팁**을 추가. 인용한 명령은 전부 실재 확인(랜딩↔실제 일치). README는 npm 패키지에 실려 npm·GitHub 양쪽 반영
- **1.37.0** — 첫 화면에 **날마다 도는 "오늘의 팁"**(35개+ 기능 발견 → 실사용·공유): 기능이 아무리 많아도 **몰라서 못 쓰면** 가치가 없다. 인터랙티브 첫 화면 배너에 `💡 오늘의 팁: 내 퇴직금 얼마? → wonjang 퇴직금 300 3`처럼 14개 팁을 **연중 일수로 골라** 하루마다 보여준다(공휴일 연차꿀팁·기념일 카드·로마자·자소서 글자수·자동차세·전월세·야근수당·전통단위 등 잘 안 알려진 기능 위주). dimmed로 은은하게, 결정론적(같은 날 같은 팁)이라 검증 가능. 풍부한 기능을 자연스럽게 발견하게 해 사용·전염으로 잇는다. `tip_for_day` 순수 함수 + 단위 테스트(전 날짜 유효·회전·결정론·모듈로 안전), 357 통과
- **1.36.1** — 단위변환에 **한국 전통 길이(자·치)** — 전통 단위 family 완성: `변환`에 **자**(30.3cm)·**치**(3.03cm)를 추가. `변환 7 자` → `212.1cm (2.12m)`. 옷감·맞춤 이불·커튼·한복은 한국에서 자로 재는데("킹 이불 7자") GPT는 cm 환산을 자주 틀린다 — 결정론적. 무게(돈·근·관)·부피(되·말)에 이어 길이까지 **한국 전통 단위 3종(무게·부피·길이) 완성**. 기존 `변환` 명령 확장이라 새 명령 0(clap 비용 없음). 단위 테스트(7자=212.1cm·1치=3.03cm)·기존 단위 회귀, 356 통과
- **1.36.0** — **한글 이름 → 로마자**(`로마자` — 여권·항공권 영문이름, 여행자 필수): `로마자 홍길동` → `Hong Gildong`. 성씨는 여권에 흔히 쓰는 **관용 표기**(김 Kim·이 Lee·박 Park·최 Choi… 40여 개), 이름은 **표준 로마자**(국립국어원, 자모 분해·자음동화 미반영). 복성(남궁·황보 등)도 인식 → `남궁민 Namgung Min`. 여권 영문이름은 모두가 한 번은 찾는데 GPT는 성 관용표기·자모 변환을 자주 틀린다 — wonjang은 유니코드 한글 음절 공식으로 결정론적(홍길동·김민준·이서연 등 실측 검증). 요율·데이터가 아니라 표기법이라 staleness 없음. `로마`(숫자)와 별개 명령, 회귀 확인. `romanize`·`surname`·`capitalize` 순수 함수 + 단위 테스트(자모 분해·관용 성씨), 355 통과
- **1.35.0** — **기념일 공유 카드**(`기념일` — 커플이 "우리 1000일 💕"을 카톡에 캡처해 자랑): 사귄 날(금연일·입사일 등)부터 며칠째인지를 자랑 카드와 같은 박스로. `기념일 2024-01-01 우리` → `💕 우리 · 888일째 · 2024-01-01부터 · 다음 1000일까지 D-112`. 커플은 한국에서 100·1000일을 가장 많이 공유하는데 그동안 [[1.26.2]]는 텍스트뿐이었다 — 캡처해 자랑할 한 장 카드로(전염 풋터 `나도 만들기 → wonjang-agent` 동봉, `--복사`·`--폭 34`). D-day 카드와 박스 시스템을 `render_event_card`로 일원화(중복 제거, Karpathy). 카톡 폭(34)·40에서 ♥ 든 이름까지 wcwidth 독립 교차검증(불변식 유지), `days_since` 재사용, 352 통과
- **1.34.0** — **야근·휴일 수당 계산기**(`야근수당` — "내 야근수당 제대로 받나?" 노동권 유틸): 통상시급으로 연장·야간·휴일 수당을 계산한다. `야근수당 12000 --연장 3 --야간 2 --휴일 8` → `연장 54,000 + 야간 12,000 + 휴일 144,000 = 210,000원`. 근로기준법 가산율(연장 1.5배·야간 +0.5 가산·휴일 1.5배), 시간 유형은 플래그로 깔끔히. 독립 검산 일치. **퇴직금·연차처럼 법 공식이라 staleness 없음**, GPT가 가산율 조합을 자주 틀리는데 결정론적. 노동 계산기 가족(퇴직금·연차·야근수당) 완성. `overtime::calc` 순수 함수 + 단위 테스트(유형별·연장만), 실측, 352 통과
- **1.33.0** — **자동차세 계산기**(`자동차세` — 차 소유자가 "내 차 세금 얼마?", 자동차 도메인): 비영업 승용차 배기량·차령으로 연 세액을 계산한다. `자동차세 1998` → `자동차세 399,600 + 지방교육세 119,880 = 연 519,480원, 6·12월 각 259,740원`. cc단가(1,000cc↓ 80 / 1,600cc↓ 140 / 초과 200원) + 지방교육세 30% + 차령 3년부터 5%/년 경감(최대 50%). 잘 알려진 실제 값과 다중 대조 검증(2000cc 신차 52만 등)·독립 검산 일치. **퇴직금·연차처럼 요율표가 아니라 안정적 법정 단가라 staleness 거의 없음**, GPT가 cc구간·교육세·차령경감을 자주 틀리는데 결정론적. `annual_tax` 순수 함수 + 단위 테스트(신차 실제값·차령 경감·상한), 실측, 350 통과
- **1.32.0** — **연차 휴가 일수 계산기**(`연차` — 모든 직장인이 헷갈리는 "내 연차 며칠?", 노동 도메인): 근속 연수로 법정 연차를 계산한다. `연차 5` → `발생 연차 17일 · 7년차에 18일로 늘어요`. 근로기준법 제60조: 1년 미만 월 1일(최대 11)·1년 이상 15일·3년부터 매 2년 +1일(최대 25). 다음 증가 시점도 안내. **퇴직금처럼 요율표가 아니라 법 공식이라 staleness 없음**, GPT가 가산 규칙을 자주 틀리는데 결정론적. 1.29.0 황금연휴와 짝(연차 며칠 ↔ 언제 쓰면 황금연휴). **빌드 안정성 동반 수정:** 서브커맨드가 늘며 clap-derive의 `command()` 디버그 스택 프레임이 기본 2MB 테스트 스레드 스택을 넘겨 별칭 파싱 테스트가 stack overflow하던 것을, 바이너리 메인 스레드와 같은 8MB 스택에서 파싱하도록 고침(릴리스 바이너리는 영향 없었음, 실측 확인). `annual_leave_days`·`next_increase` 순수 함수 + 단위 테스트(법정 일수·증가 시점), 실측, 348 통과
- **1.31.0** — **퇴직금 계산기**(`퇴직금` — 이직·퇴사 시 누구나 계산, 노동 도메인): 월 평균임금·근속기간으로 법정 퇴직금을 추정한다. `퇴직금 300 3` → `약 888만원 (8,876,712원)`. 퇴직금 = 1일 평균임금 × 30일 × (재직일수/365), 1일평균 ≈ 월급×12/365. **전기요금과 달리 요율표가 아니라 근로기준법 공식이라 staleness 없음** — 고용노동부 예시(월급 300만·3년 = 8,804,348원)와 **0.83% 이내로 검증**. 1년 미만은 "법정 대상 아님" 안내, 평균임금에 상여·연차수당이 더해져 실제는 더 많을 수 있다는 추정 disclaimer. GPT가 평균임금·재직일수 계산을 자주 틀리는데 결정론적. `severance_manwon`·`service_days` 순수 함수 + 단위 테스트(1년≈1달치·일수 비례·고용노동부 1% 이내·일수 환산), 실측, 346 통과
- **1.30.0** — **전월세 전환 계산기**(`전월세` — 전세↔반월세, 부동산 도메인): 전세보증금을 (반)월세로 돌릴 때 월세를 계산한다. `전월세 30000 5.5 10000`(전세 3억·전환율 5.5%·보증금 1억) → `보증금 1억 → 월세 약 91.7만원 (916,667원)`, 순수 월세(보증금 0)도 함께. 월세 = (전세−보증금)×전환율/12, 억/만원 한국식 표기. 부동산 사이트마다 있는 계산이지만 GPT는 공식·억 단위 산수를 자주 틀린다 — wonjang은 결정론적(독립 검산 일치). 금융 계산기 가족(대출·예금·실수령)에 합류. 가드(전세 0 이하·전환율 음수·보증금>전세금 거부) + `monthly_rent`·`fmt_eok` 순수 함수 단위 테스트, 실측, 342 통과. 법정 상한(기준금리+2%) 안내 동봉
- **1.29.1** — `글자수`에 **자소서 제한 체크**(`--제한`, 취준생이 매일 쓰는 "오 편하다"): 한국 자소서는 "공백 포함 1000자 이내"처럼 **제한**이 핵심인데 그동안 세기만 하고 남은 글자를 안 알려줬다. 이제 `글자수 "..." --제한 1000`이면 `공백 포함 936자 (제한 1000 → 64자 남음)`, 초과 시 `⚠️ 50자 초과`를 보여준다 — **포털마다 공백 포함/제외 기준이 달라 둘 다** 표시(사람인·잡코리아 등 어디에 내든 맞춤). 딱 제한이면 0자 남음(초과 아님). 제한 없이 쓰면 종전과 동일(하위호환). `char_limit_note` 순수 함수 + 경계 단위 테스트(남음·딱맞음·초과·미지정), 실측, 339 통과
- **1.29.0** — `공휴일`이 **연차 하나로 만드는 황금연휴**를 추천한다(연말마다 가장 많이 공유되는 정보): 공휴일·다음 연휴에 더해 `💡 연차 하나로 황금연휴: 09-23(수) 연차 → 5일 연휴 (09-23~09-27)`처럼 **징검다리 연차**를 길이 순 상위 3개로. 평일 하루를 쉬면 앞뒤 휴일·주말과 이어져 4일+ 연속이 되는 날을 결정론적으로 계산(실측: 2026 추석 앞뒤 09-23·09-28 연차 → 각 5일, 광복절 08-14 연차 → 4일). "연차 어떻게 써야 황금연휴?"는 한국에서 매년 검색·공유 폭발하는데 GPT는 대체공휴일·요일 끼임을 자주 틀린다. 제헌절 제외 로직은 `off_holiday_set`으로 [[1.27.0]] 연휴 계산과 일원화. `golden_leaves` 순수 함수 + 단위 테스트(광복절 양쪽 연차 4일·공휴일 없으면 없음), 실측, 338 통과
- **1.28.1** — 데이터 없을 때 `자랑`이 **예시 카드를 보여준다**(첫인상→전염 전환): 호기심에 `wonjang 자랑`을 처음 친 신규 사용자는 데이터가 없어 텍스트 안내만 받았다 — 결과물을 못 보니 "갖고 싶다"가 안 생겼다. 이제 **예시 카드**(15일 연속 🎉·잔디·집중·지출·수능 D-150·일기)를 먼저 렌더해 "한 달만 채우면 이런 카드가 생겨요"로 보여주고 습관 등록을 권한다. 제목 `예시`로 본인 데이터와 명확히 구분. 수집→자랑→전염 깔때기의 **맨 윗단 누수**를 막음. 예시 카드도 박스 폭 불변식 유지(폭 46 10줄 wcwidth 독립검증), 336 통과
- **1.28.0** — **D-day 공유 카드**(`디데이 카드` — 수능 D-100을 카톡에 캡처해 자랑): 한국 학생·군인이 미친듯이 공유하는 "수능 D-166"·"전역 D-268" 카운트다운을, 자랑 카드와 **같은 박스 시스템**으로 한 장 카드로. `wonjang 디데이 카드 [이름]`(생략 시 가장 가까운 디데이), `--폭 34`(카톡)·`--복사`(클립보드). 박스 폭 불변식은 자랑 카드와 동일하게 보장 — 폭 30~52 전 구간 wcwidth 독립 교차검증(가운데 정렬·이모지 포함 9줄 정확 일치). 풋터 `나도 만들기 → wonjang-agent`로 받은 사람도 설치 경로를 안다(전염 고리). `render_dday_card`·`centered` + 단위 테스트(폭 불변·카톡 풋터 보존), 실측, 336 통과
- **1.27.0** — `공휴일`이 **다음 연휴 며칠 연속인지** 알려준다(연휴 캘린더, 한국에서 가장 자주 공유): 공휴일 목록·다음 빨간날 D-day에 더해 `🏖️ 다음 연휴: 08-15(토)~08-17(월) 3일 연속 (D-70)`을 보여준다. "이번 추석 며칠 쉬어?"·"다음 연휴 언제?"는 한국에서 매일 나오는데 GPT는 **대체공휴일·주말 끼임**을 자주 틀린다 — wonjang은 주말+공휴일 연속 구간을 결정론적으로 계산(광복절이 토요일이면 월요일 대체공휴일로 3일 연휴까지 정확). **검증 중 발견·수정:** Nager API가 제헌절(쉬는 날 아님, 2008~)을 다른 공휴일과 똑같이 `Public`으로 줘 가짜 연휴(07-17 금~일)를 만들던 걸 제외 처리. `next_long_break` 순수 함수 + 단위 테스트(추석 4일·월 공휴일 3일·주말만 None·제헌절 제외), 실측, 335 통과
- **1.26.3** — `나이`에 **살아온 날수 + 다음 천일 기념일**(한국에서 챙기는 '날수' 자랑거리): 만나이·띠·별자리·다음 생일에 더해, `🗓️ 살아온 날: 13,171일`과 `🎉 14,000일까지 D-829 (2028-09-12)`을 보여준다. "나 1만일 넘었네"는 사람들이 실제로 자랑·공유하고, 아기는 1000일이 **천일잔치**로 딱 떨어진다(실측: 2025-01-01생 → 1,000일까지 D-479). 정확한 날짜 산수는 GPT가 자주 틀리는데 wonjang은 결정론적. `days_lived`·`next_day_milestone` 순수 함수 + 단위 테스트(살아온 날·1000일 마디·미래생일 None), 331 통과
- **1.26.2** — `날짜 <하루>`가 **며칠째 + 다음 기념일**을 알려준다(커플·기념일, 한국에서 가장 자주 공유되는 날짜 계산): 예전엔 날짜 하나를 주면 "올해 N번째 날"(연중 일수, 게다가 다른 해엔 '올해'가 어색)만 떴다. 이제 **과거 날짜면** 한국식 카운팅(시작일=1일째)으로 `오늘로 887일째 (886일 전)`와 **다가오는 마디 2개의 양력 날짜**(`🎉 1000일 → 2026-09-26 (D-113)`, 1년·2년… 포함)를, **미래 날짜면** `D-203`을, 오늘이면 연중 일수를 보여준다. "우리 사귄 지 며칠?"·"100일 언제야?"는 한국에서 매일 나오는데 GPT는 날짜 산수를 자주 틀린다 — wonjang은 결정론적으로 정확. 자연어도 에이전트가 `날짜 <사귄날>`로 잇도록 프롬프트·도움말 갱신. 두 날짜·N일후 기능은 그대로(회귀 확인). `days_since` 순수 함수 + 단위 테스트(한국식 N일째·100일 양력), 실측, 330 통과
- **1.26.1** — 자랑 카드는 **성취만**(0인 항목 줄 빼기 → 더 자랑할 맛 나게): 월간 자랑 카드가 `집중 0분`·`지출 0원`·`✍️ 일기 0번`을 **0이어도 항상 표시**해, 막 시작한 활성 사용자(습관·집중은 했지만 가계부·일기는 아직)의 카드를 0으로 어지럽혔다 — 공유를 시작할 바로 그 시점에 자랑할 맛을 깎았다. 이제 streak·잔디·D-day처럼 **값이 있을 때만** 줄을 넣어 가진 성취만 깔끔하게 보여준다(`has_data`가 빈 카드는 이미 차단). 카톡 폭 불변식은 그대로 — 폭 30~52 전 구간 wcwidth 독립 교차검증. `focus_label`·`expense_label`을 `Option`으로(dday와 동일 패턴), 0-숨김 단위 테스트, 328 통과
- **1.25.2** — 코인 헛심볼 에러 **영문 내부 오류 누출 차단**(첫인상 표면 정직화): `wonjang 코인 ZZZZZZ`처럼 없는 심볼을 넣으면 업비트가 배열 대신 에러 객체를 돌려주는데, 그대로 역직렬화하다 serde 내부 오류 `"...invalid type: map, expected a sequence at line 1 column 0"`를 **영문 그대로 사용자에게 노출**했다(다른 명령은 다 깔끔한 한국어인데 코인만 깨진 느낌). 응답을 텍스트로 받아 배열로 안 풀리면 내부 오류를 감추고 `"'ZZZZZZ' 코인을 찾을 수 없어요. 심볼을 확인해 주세요 (예: BTC, ETH, XRP)"`로 안내. 라이브 명령 8종 오타 경로를 전수 점검해 **유일하게 새던 곳**을 막음. `parse_tickers` 순수 함수로 분리해 단위 테스트(정상 배열·에러 객체에 serde 내부 문자열 불포함·빈 배열 방어), 정상 시세 회귀 확인, 312 통과
- **1.25.1** — 지하철 도착 안내 **대괄호 잔재 제거**(가장 많이 캡처되는 실시간 화면의 미세 다듬기): 서울 열린데이터 API의 `arvlMsg2`는 남은 역 수를 `"[3]번째 전역 (종로5가)"`처럼 **대괄호로 감싸** 내려보내, 사용자에겐 마크업 찌꺼기처럼 보였다. 대괄호 **안이 숫자뿐일 때만** 벗겨 `"3번째 전역 (종로5가)"`로 정리(그 외 `"전역 도착"`·`"2분 30초 후"` 등은 그대로). 지하철 도착은 캡처·공유가 잦은 표면이라 사소해 보여도 **첫인상 완성도**에 직결. `clean_arvl_msg` 순수 함수로 분리해 단위 테스트(숫자 대괄호만 제거·그 외 불변·빈 문자열 방어), 실측으로 `[N]` 잔재 0 확인, 309 통과
- **1.20.2** — 자랑 카드 **마일스톤 뱃지**: 가장 긴 연속이 7·30·100·365일을 넘으면 등급 뱃지(🎉→🎊→🏆→👑)를 streak 줄에 붙여, 공유된 카드만 봐도 **성취 수준이 한눈에**(사회적 화폐 — 자랑할 맛↑). [[1.20.1]] 습관 done 마일스톤 트리거와 짝(트리거→카드 뱃지로 일관). **카드 폭 불변식은 절대 못 깨므로** 카톡 폭 34 포함 30~60 전 구간 wcwidth 독립 교차검증(🏆 든 줄도 정확히 폭 일치 확인). `streak_badge` 등급 단위 테스트, 303 통과
- **1.20.1** — 습관 마일스톤 축하 + 자랑 유도(전염 트리거): `습관 done`으로 연속 **7·14·30·50·100·200·365일**을 달성하면 "🎉 운동 일주일 연속 달성! 이 기록, 카드로 남겨 자랑해요 → wonjang 자랑 --복사"를 띄운다. 사람이 가장 공유하고 싶은 **감정의 정점(마일스톤)**에 자랑 카드를 떠올리게 하는 트리거(STEPPS '계기'·'감정'). 흔치 않은 고비에서만 떠 매일 스팸이 되지 않음(평범한 날은 조용). `habits::milestone` 순수 함수로 분리해 단위 테스트(고비만 발화·평일 None), 실측(7일째 축하·1일째 조용), 302 통과
- **1.14.12** — 광고된 '다정' 성격을 실제로 고를 수 있게(README↔명령 불일치 수정): README·기능표가 비서 말투를 **'다정·친구·집사·선배·발랄'**로 안내하는데, 정작 `wonjang 성격 다정`은 "'다정' 성격은 없어요" 에러였다 — 기본 페르소나의 키가 `기본`, 표시명이 `다정한 비서`라 '다정'으로는 선택이 안 됐던 탓. 광고한 이름 그대로 되도록 **'다정'을 기본(다정한 비서) 페르소나 별칭으로** 받아들인다(알리아스 보강과 같은 결). 5종 페르소나 인사가 실제로 각각 뚜렷함도 확인(친구=반말·집사=주인님·선배=시크·발랄=✨·다정=존댓말), 회귀 테스트 추가, 295 통과
- **1.14.10** — README에 자랑 카드 대표 노출(전염 전환율 누수 차단): 공유된 카드 풋터(`나도 만들기 → wonjang-agent`)를 보고 검색해 찾아온 사람이 정작 **README에서 자랑 카드를 한 글자도 못 보던** 깔때기 누수를 수정. 상단에 **실제 렌더 예시 카드(15일 잔디·집중·지출·D-day) + 설명**을 히어로로 넣고, '기능 한눈에' 표 맨 위(AI 다음)에도 자랑 카드 행 추가. 카드를 보고 온 사람이 곧장 "이게 그거구나" 확인하고 설치하도록. README는 npm 패키지에 실려 **npm·GitHub 랜딩 모두** 반영. 예시 카드는 대표 데이터로 실제 렌더해 박스 정렬 정확
- **1.14.4** — 자랑 카드 성장 고리 풋터(전염성↑): 카드를 공유받은 사람에게 카드는 **'이게 뭔지·어떻게 받는지'를 알려줄 유일한 표면**이다. 하단 테두리 풋터를 내부 버전(`wonjang · v1.14.3` — 받은 사람에겐 의미 없는 숫자)에서 **`나도 만들기 → wonjang-agent`**(npm 설치명 + 부드러운 권유)로 교체 — 카드를 본 사람이 곧장 검색·설치할 단서를 카드 자체가 품는다(STEPPS '공개성'·실용가치). 월간·주간 카드 공통, `card::SHARE_FOOTER` 단일 상수. 카톡 폭(34)에서도 설치명이 안 잘리도록 박스 폭 불변식 + 설치명 보존을 단위 테스트하고, 렌더 결과를 독립 `wcwidth`로 교차검증(모든 줄 폭 34 정확 일치)
- **1.14.0** — 일요일 저녁 주간 결산 선톡(선톡 × 전염): 24시간 데몬이 **일요일 19시 이후 주 1회** "📊 이번 주 결산! 🔥운동 7일 · 🍅집중 11시간▲2시간 · 💰240,000원▼50,000원 · 'wonjang 자랑 --주'로 카드 보기 👀"를 먼저 푸시. 자랑 카드를 **주말마다 떠올리게 하는 고빈도 트리거**. 결산 문구는 순수 함수로 단위 테스트(데이터 없으면 안 보냄), 데몬 게이트는 검증된 공휴일·습관 알림과 동일 패턴. 푸시 채널 설정 시
- **1.13.1** — 자랑 카드 원클릭 복사(`자랑 --복사`): 카드를 **플레인 텍스트로 클립보드에 바로 복사**(파이프 `| pbcopy` 없이) — 카톡·메모에 붙여넣기만 하면 됨. 공유 마찰을 줄여 전염성↑. 월간·주간 카드 공통. macOS pbcopy/리눅스 xclip 재사용
- **1.13.0** — 🌟 **주간 자랑 카드(`자랑 --주`)**: 월간 카드에 이어, 이번 주 기록 + **지난주 대비 ▲▼ 변화**를 한 장 카드로(🍅 집중 11시간 ▲2시간, 💰 지출 240,000원 ▼50,000원). 진보가 한눈에 보여 주말마다 자랑·공유하기 좋은 **고빈도 전염 트리거**(비전 패널이 꼽은 주간 회고). 월간과 같은 전각폭 박스 시스템 재사용 — `▲▼` 포함 모든 줄 동일폭을 wcwidth 독립 교차검증. 페르소나별 응원 코멘트
- **1.10.0** — 첫인상 와우 + 응집: 무인자 실행 배너에 **오늘까지의 미니 잔디** 한 줄 추가 — 습관이 있으면 `🔥 운동 7일째 ▓▓▓▓▓▓▓`(초록), 없으면 빈 잔디 + "습관 하나만 등록하면 한 달 뒤 자랑 카드가 생겨요". 온보딩 카피를 '도구 나열'에서 **'수집→자랑 루프'**로 교체(배너→온보딩→`자랑`이 한 내러티브). `현황` 대시보드가 사용자가 고른 **성격(페르소나)을 무시하던 버그 수정**(greeting() 중복 제거 → soul::greeting() 단일화)
- **1.9.0** — 🌟 **자랑 카드(`자랑`)**: 이번 달 내 기록(가장 긴 습관 연속·최근 4주 잔디 ▓░·집중 시간·지출·다가오는 D-day·일기)을 **카톡 코드블록에서도 안 깨지는 한 장 ANSI 회고 카드**로. 한글·이모지 전각폭(2칸)을 정확히 계산해(`unicode-width`) 박스가 절대 어긋나지 않음 — 독립 `wcwidth` 교차검증으로 모든 줄 동일폭 확인. 페르소나별 코멘트(친구·집사·선배·발랄), `--폭 34`(카톡)·`--달 YYYY-MM`·`--no-color`. 데이터가 없으면 "습관 하나 시작해봐요" 안내. **PNG는 의도적으로 안 만듦**(한글 TTF가 정적 바이너리를 비대화 — 빌드정책 준수). 단톡방·SNS에 캡처해 자랑하기 좋은 전염성 기능(41-에이전트 비전 패널 5/5 수렴)

## 안정성·품질

- **1.36.2** — 최근 명령 엣지 적대 검증으로 발견한 2건 수정(verify-or-don't-ship): 새로 추가한 명령들을 0·음수·거대값·비한글로 전수 점검하다 — ① **자동차세 거대 차령 정수 오버플로**: `자동차세 1998 999999999`에서 차령 경감 `(차령−2)×5`가 `u32`를 넘겨 디버그 빌드는 패닉, 릴리스는 wrap 후 `.min(50)` 덕에 우연히 맞던 것을, **곱하기 전 10으로 캡**(`(차령−2).min(10)×5`)해 오버플로 자체를 제거(거대 차령도 정확히 50% 상한). ② **로마자 비한글 입력**: `로마자 hello`가 "H Ello" 같은 엉뚱한 출력을 내던 것을, 한글이 없으면 "한글 이름을 입력하세요"로 친절 거부. 나머지(퇴직금·연차·야근수당·기념일 엣지)는 전부 견고 확인. `u32::MAX` 차령 단위 테스트로 오버플로 방어 고정, 356 통과
- **1.25.9** — 에이전트가 카카오 알림을 알도록(프롬프트↔실제 채널 일치, 프롬프트 전수 점검으로 발견): 에이전트 시스템 프롬프트의 모든 **명령 이름·플래그를 실제 `--help`와 전수 대조**(`--세로` 류 불일치 재발 방지)한 결과 — 명령·플래그는 전부 정확했고, 단 하나 **notify 채널 설명이 `디스코드/슬랙/텔레그램`으로 한국 1순위 `카카오`를 빠뜨린 것**을 발견. 실제 `configured_channels`는 4채널 모두 지원하는데 프롬프트만 카카오를 몰라, AI가 "카카오로 보내줘"를 놓칠 수 있었다(한국 제품에 가장 중요한 채널). 4채널 모두 안내하도록 고치고 "채널을 짚어도 같은 명령" 힌트 추가. 데몬 알림 경로의 stale 주석(`디스코드/텔레그램`)도 4채널로 정정. `system_prompt`에 카카오 포함 4채널 회귀 테스트, 325 통과
- **1.25.8** — 프리셋을 이름으로 바로 실행(도움말↔실제 일치, 도움말 명령 전수 점검으로 발견): `도움`에 적힌 91개 명령을 **전수 점검**(각 `--help`로 등록 여부 확인)하다, `일지/메모 (프리셋)`만 실제론 미등록이라 `wonjang 일지`가 프리셋이 아니라 **일반 AI로 새던 것**을 발견 — 도움말은 직접 호출을 암시하는데(다른 곳은 멀쩡), 실제론 `preset run 일지`로만 됐다. 이제 단발 입력의 **첫 단어가 프리셋 이름/별칭과 정확히 일치하면** 그 프리셋으로 해석(`wonjang 일지 오늘 운동함` → 옵시디언 일지 기록, 나머지는 '추가 지시'로). 클랩 명령이 먼저 파싱되므로 이름이 겹치는 프리셋(압축·날씨 등)은 명령이 가져가 **충돌 없음**, 정확 일치라 자연어 오인도 없음. `resolve_bare_preset` 순수 함수 + 단위 테스트(이름·별칭·추가지시·자연어 제외), 실측(일지·메모 프리셋 발동·날씨는 명령 유지·자연어 그대로), 324 통과
- **1.25.7** — 타임스탬프 변환의 **영문 요일 → 한국어**(영문 날짜 잔재 전수 소탕): 영문 날짜 토큰을 소스 전체에서 체계적으로 훑다(`%a %b` 등·RFC passthrough·영문 요일/월), `타임스탬프 1700000000`의 로컬 시각이 `2023-11-15 07:13:20 (수)`가 아니라 **`(Wed)`** 영문 요일로 찍히던 마지막 한 곳을 발견. 기존 `datecalc::weekday_kr`를 재사용해 한국어 요일로(중복 구현 없이). **전수 점검 결과 이 한 곳이 마지막**이었고(세계시간·시차는 이미 한국어 요일 사용), 환율 KST(1.25.6)와 함께 **날짜·시각 표면의 영문 잔재가 0**이 됐다. 회귀 테스트(로컬 문자열에 `(화)` 포함·`Tue` 불포함), 실측 확인, 320 통과
- **1.25.6** — 환율 갱신 시각을 **영문 UTC → 한국시간(KST)**으로(한국어 일색 출력의 영문 날짜 제거, 실측으로 발견): `환율 100 USD`의 갱신 시각이 `(Thu, 04 Jun 2026 00:02:31 +0000)`처럼 **영문 RFC-2822·UTC** 그대로 찍혀, 나머지 한국어 출력 속에서 이질적이고 한국 사용자에겐 UTC라 시점도 헷갈렸다(open.er-api.com 원본 문자열 패스스루). 한국 사용자에게 가장 쓸모 있는 **KST 기준**으로 파싱·변환해 `2026-06-04 09:02 기준 (KST)`로 표기(코인 영문 오류·지하철 대괄호에 이은 "한국어 우선 표면 정직화"). 파싱 실패 시 원본 보존(형식 변화 방어). `format_update_time` 단위 테스트(UTC→KST·날짜 넘김·실패 시 원본), 실측 확인, 320 통과
- **1.25.5** — 자연스러운 한국어 단어를 기능 별칭으로 보강(가장 흔한 말이 곧장 기능으로, 실측으로 발견): 상태 명령을 훑다 **`북마크`**(즐겨찾기의 외래어이자 가장 흔한 말)를 입력하면 즐겨찾기 기능 대신 **AI로 위임**돼 — 느리고 예측 불가하며 백엔드 미연결 시 아예 실패 — 기능을 통째로 놓치던 걸 발견. 자연어 단어를 전수 점검해, 등록된 단일 기능에 **모호함 없이 매핑되는 외래어·약어**를 별칭으로 추가: `북마크`·`즐찾`→즐겨찾기, `타이머`→집중(뽀모도로), `리마인더`→약속/알림, `투두`→할일. (모호한 `일정`·`메모` 등은 의도적으로 제외 — AI 위임이 더 맞음.) [[1.14.12]] '다정' 별칭 보강과 같은 결(광고·직관대로 되게). `Cli` 파싱 테스트로 새 별칭이 로컬 명령으로 바인딩됨을 고정(+기존 별칭 회귀), 실측(5개 별칭 로컬 처리·기존 유지), 319 통과
- **1.25.4** — 내보내기 파일을 **다운로드 폴더로**(어디 저장됐는지 알 수 있게, 실측으로 발견): `디데이 내보내기`·`지출 내보내기`가 `--출력` 생략 시 **현재 폴더(cwd)에 상대 이름**(`디데이.ics`·`가계부.csv`)으로 떨궈, ① 사용자가 **어디 저장됐는지 모르고**(메시지에도 경로가 안 보임), ② 깃 저장소처럼 **중요한 폴더에서 실행하면 그 폴더를 어지럽혔다**(실측 중 레포에 파일이 생긴 걸 발견). `.ics`/`.csv`는 받아서 더블클릭으로 캘린더·엑셀에 넣는 파일이라 **브라우저 내려받기처럼 다운로드 폴더**에서 찾는 게 자연스럽다 — 기본 저장 위치를 다운로드 폴더로 바꾸고 **절대 경로를 메시지에 표시**(CSV 분석 힌트도 실제 경로로 복붙 가능). `--출력`은 그대로 존중, 다운로드 폴더가 없으면 종전대로 cwd. `pick_export_path` 순수 함수로 분리해 단위 테스트(폴더 있으면 그 안·없으면 cwd), 실측(다운로드 폴더 저장·`--출력` 존중·cwd 미오염·.ics 형식 유효), 317 통과
- **1.25.3** — 계산기 엣지 입력 정직화 2차([[1.23.3]] 원칙 확장, 실측으로 발견): 라이브 명령·계산기를 0·음수·`inf`로 전수 실측하다, `실수령 0`·`부가세 0`·`시급 X 0`이 **무의미한 0원 내역**을 그대로 출력하던 것을 발견 — 특히 `실수령 0`은 공제 항목이 **`-0원`**(마이너스 0)으로 찍혀 깨진 느낌. v1.23.3이 나이·대출에 세운 "무의미 출력 거부" 원칙을 이 세 곳에도 적용: 비유한·0 이하 입력이면 `"연봉은 1만원 이상이어야 해요"`·`"금액은 0보다 커야 해요"`·`"시급과 주당 근무시간은 0보다 커야 해요"`로 친절 거부(정상 입력은 회귀 확인). `inf`/`NaN`도 함께 차단(엑셀 셀 inf/nan 방어와 같은 결). `cmd_salary`·`cmd_vat`·`cmd_wage` 가드 단위 테스트(0·음수·비유한 거부, 정상 통과), 315 통과
- **1.24.6** — `이미지이어붙이기 --세로` 플래그 추가(도움말·에이전트 프롬프트 정확성): 도움말·README·에이전트 시스템 프롬프트가 `--세로`(기본)를 안내했지만 **실제로는 `--세로` 플래그가 없어**(세로는 무플래그 기본) `--세로`를 입력하면 `unexpected argument` 에러였다 — 에이전트가 프롬프트대로 따라 하면 실패. 명시적 `--세로` 플래그를 추가해 `--가로`와 대칭으로(세로=기본, 둘 다 주면 `--세로` 우선). 직전 v1.24.5의 프롬프트를 실측 검증하다 발견. 308 통과
- **1.24.5** — 에이전트가 새 빌트인 능력을 쓰도록 시스템 프롬프트 갱신(자연어↔빌트인 연결): 최근 추가한 강력한 기능들이 에이전트 시스템 프롬프트에 없어, 사용자가 **자연어로** 요청해도 에이전트가 검증된 빌트인을 모르고 약한 방식으로 처리하던 것 보완. 엑셀 분석에 **`--그룹`(피벗)·`--필터`·`--정렬`·`--저장`·`--시트`**를 명시하고, **`표합치기`(여러 표 병합)·`이미지이어붙이기`(긴 캡처)·`디데이 내보내기`(.ics)·`지출 내보내기`(가계부→CSV→분석)**를 추가. 이제 \"지점별 매출 합계 내줘\"·\"가계부 분석해줘\"·\"캡처들 이어줘\"·\"디데이 캘린더에 넣어줘\" 같은 자연어가 그 빌트인 도구를 직접 활용. 308 통과
- **1.24.4** — 적대 감사 마무리(텔레그램 기록 상한·엑셀 큰 정수): ① **[23] 텔레그램 채팅 기록 무한 증가** — chat별 대화 기록이 끝없이 쌓여 컨텍스트 초과로 일정 시점부터 **모든 응답이 "작업 중 오류"**가 되던 것을(데몬 재시작 전까지 회복 불가), 슬라이딩 윈도우(시스템 메시지 보존 + 최근 30개, 도구호출/결과 짝이 안 깨지게 `user` 턴 경계에서 자름)로 상한. ② **[4] 엑셀 큰 정수 손상** — i64 범위(약 9.2e18) 넘는 정수 셀을 `as i64`가 i64::MAX로 포화시켜 **조용히 다른 값으로 손상**하던 것을(경 단위 금액·19~20자리 ID 등), 안전 범위 밖은 원본 그대로 포맷. 회귀 테스트, 308 통과
- **1.24.3** — 게이트웨이·백엔드 보안·견고성(적대 감사 [21][18]): ① **텔레그램 무한 핫루프 차단** — `getUpdates`가 API 오류(409 Conflict 등, 같은 봇 토큰으로 다른 곳에서 폴링)를 `ok:false`로 **즉시** 받으면 빈 결과처럼 흘려, sleep 없이 `getUpdates`를 무한 반복(CPU 100%·텔레그램 API 폭주)하던 것을 수정 — `ok=false`면 Err로 올려 5초 백오프 재시도(`UpdatesResp`에 `ok`·`description` 추가, 회귀 테스트). ② **[보안] Codex 백엔드 읽기전용 미적용** — `codex exec`가 wonjang의 읽기전용 의도(비-`-y` 모드)를 무시하고 파일 쓰기·명령을 실행하던 것을, Claude 백엔드처럼 `--sandbox`로 제한(비자동승인=`read-only`, 자동승인=`workspace-write`). codex 미설치 환경이라 플래그는 OpenAI Codex CLI 표준 기준이며 미지원 버전에선 안전하게 실패(무제약 실행 없음). fmt·clippy(-D warnings)·307 테스트 통과
- **1.24.2** — 데몬 저장 실패 시 재실행·재발동 폭주 방지(무인 24h 견고성, 적대 감사 후속): 데이터 디렉터리가 읽기전용이거나 디스크가 가득 차 `atomic_write`(rename)가 실패하면 — ① **예약 작업**(`cron run`)의 `last_run_ms`가 디스크에 안 남아 매 틱(30초)마다 같은 작업을 **재실행**(LLM 호출·푸시 폭주), ② **시세 알림**(코인·환율)도 `triggered` 표시가 안 남아 30초마다 같은 알림을 **재발동**하던 것 수정. 둘 다 실행/발동 기록을 **인메모리로도 보관**해 매 틱 reload되는 store에 병합(디스크 저장 실패분을 메모리가 보존). **읽기전용 디렉터리 주입 테스트로 실측** — 저장 실패해도 작업은 1회만 실행(폭주 0, 75초/여러 틱 검증), 306 통과
- **1.24.1** — 적대 감사 후속 2배치(정수 오버플로·금융 음수·inf/nan): ① **정수 오버플로 saturating화** — 리마인더/집중 분→초(`minutes*60`)·cron 스케줄 파서(`n*60/3600/86400`)·에이전트 add_reminder 도구·더치페이 곱셈·`handle_fired` 재예약 루프·pdf회전 `/Rotate` 누적이, 거대 입력이나 악성 PDF에서 디버그 패닉·릴리스 wrap(거대 간격이 짧은 간격으로 둔갑·총액 음수)으로 이어지던 것을 saturating/정규화로 차단. ② **금융 음수 거부** — 대출·예금·적금이 `-- -4.5`로 음수 금리가 도달하면 음수 이자·`--53,900원` 이중부호 출력을 내던 것 → "원금·연이율은 0 이상" 거부(clap 차단 가정이 `--`로 깨졌음). ③ **parse_num inf/nan** — 엑셀 셀에 영문 `inf`/`infinity`/`nan`이 있으면 `f64::parse`가 받아들여 합계·평균을 NaN/inf로 조용히 오염하던 것 → 유한값만 숫자로. 회귀 테스트 추가, 306 통과
- **1.24.0** — 13-서브시스템 적대 감사 후속(46-에이전트 워크플로 → 반증 검증 → 확정 버그): ① **[데이터 안전·핵심] 손상 항목 하나가 저장소 전체를 조용히 삭제하던 것 수정** — 모든 로컬 저장소(습관·지출·집중·디데이·약속·할일·즐겨찾기·코인감시·예약·또간집 10곳) `load`가 `unwrap_or_default()`라, JSON에 **필수 필드 빠진 항목이 하나라도 있으면 전체가 빈 것으로 읽혀** 다음 save가 파일을 통째로 덮어써 **모든 데이터가 조용히 사라졌다**(외부 편집·디스크 손상 시). 이제 손상 파일을 `*.corrupt`로 백업·보존하고 새로 시작(복구 가능, 무음 손실 0). `util::load_json_or_recover`. ② **[자랑 카드] 피부톤 이모지 폭 깨짐** — `💪🏽`·`👨🏿‍💻` 등 Fitzpatrick 피부톤 수정자를 2칸으로 이중 계산해 박스가 어긋나던 것 수정(ZWJ 수정에 이은 후속 — 클러스터가 피부톤도 흡수). 폭 30~80 wcwidth 독립검증. ③ **패닉·엣지 수정**: 날짜 `--plus` 거대값(NaiveDate 범위초과 raw 패닉 → 친절 에러), 금액 2^63 이상(u64→i64 음수 깨짐 → 직접 콤마 포맷), 긱뉴스 href 멀티바이트(바이트경계 슬라이싱 패닉 → `len_utf8`), 예금·적금 0개월(무의미 0원 → 거부, 대출과 일관). 각 회귀 테스트 추가, 306 통과
- **1.23.3** — 계산기 엣지 입력 정직화(적대적 테스트로 발견): ① **나이 미래 날짜** — `나이 2099-01-01`이 "만 나이 **-73세**" 같은 음수 나이를 내던 것 → "미래 날짜예요 — 생년월일 확인" 친절 거부. ② **대출 0개월** — `대출 10000 3 0`이 "0년 · 월 상환액=원금 전액" 무의미 출력을 내던 것 → "개월 수는 1 이상" 거부. (BMI·칼로리의 0/음수는 이미 검증돼 있었고, 할인 150%는 0원으로 올바로 캡, 음수 인자는 clap이 차단 — **실제 도달 가능한 무의미 출력만** 골라 수정.) 계산기 17종을 0·음수·미래·초고액으로 적대 검증, 305 통과
- **1.23.2** — 자랑 카드 **ZWJ 이모지 폭 버그 수정**(전염 센터피스 사수): 습관·디데이 이름에 `👨‍💻`(개발자)·`🏃‍♂️`(달리기)·`👨‍👩‍👧‍👦`(가족) 같은 **ZWJ(zero-width-joiner) 이모지**가 들어가면 `disp_width`가 폭을 과다 계산해(ZWJ로 합쳐진 글자를 따로 셈) 카드 박스 우측 테두리가 어긋나던 것 수정 — 카톡 "안 깨짐" 계약 위반이라 공유 카드(전염 단위)엔 치명적. `disp_width`/`truncate_width`를 **표시 클러스터 단위**(base+변이선택자+ZWJ 후속=한 글자)로 재작성. **적대적 스트레스 테스트로 발견**(긴 이름+여러 ZWJ 이모지), 폭 30~80 전 구간 wcwidth 독립검증, ZWJ 단위 테스트 추가, 305 통과
- **1.23.1** — 저장 플래그 일관성(`--출력`=`--저장`): 파일을 만드는 대부분 명령(사진묶기·pdf합치기·이미지·이어붙이기·디데이/지출 내보내기)은 `--출력`을 쓰는데, 엑셀 분석·표합치기만 `--저장`이라 익숙한 `--출력`을 치면 "unexpected argument" 에러가 났다. `엑셀`·`표합치기`의 저장 플래그에 `--출력`을 별칭으로 추가해 **둘 다 통함**(도움말에도 `[aliases: --출력]` 표기). 직접 만든 불일치를 바로잡음, 304 통과
- **1.14.11** — 백엔드 없는 첫인상 정직·친절(설치 직후 이탈 방지): 자연어(에이전트)만 Claude Code/Codex(또는 API 키)가 필요하고 **자랑·습관·계산기·파일도구 등은 키 없이 다 되는데**, 백엔드 없는 새 사용자에게 그게 안 보였다. ① **백엔드 못 찾음 안내** — 막다른 에러 대신 "백엔드 없이도 지금 바로 됩니다 — wonjang 자랑·습관·가계부·환율·날씨·엑셀·pdf합치기 …"를 덧붙여 키리스 가치를 노출. ② **배너 거짓 약속 제거** — 백엔드를 `claude`/`codex`로 명시했는데 그 CLI가 PATH에 없으면, 배너가 "키 없이 연결됐어요"라고 거짓말하던 것을 "💡 연결 전 — 설치·로그인하면 자연어까지(아래 기능들은 지금 바로 OK)"로(`CliKind::is_available`/`Engine::backend_ready`). 양쪽 PATH 시나리오·키리스 빌트인 동작 실측, 294 테스트 통과
- **1.14.9** — 밀린 알림 첫 실행 폭주 방지(첫인상·스팸): 집중(뽀모도로) 타이머 등은 "N분 뒤 알림"을 예약하는데, 이를 울려 줄 데몬(`cron run`)을 안 켜 두면 알림이 안 울린 채 **쌓인다**. 그러다 데몬을 처음 켜는 순간, 지난 알림이 **한꺼번에 무더기로** 울려(데스크탑+푸시) 스팸이 되던 것 수정 — 예정 시각이 **1시간 넘게 지난 알림은 조용히 처리만 하고 울리지 않는다**(`reminders::should_alert`). 반복 알림은 다음 회차로 정상 재예약돼 앞으로의 알림엔 영향 없음. 데몬 정적 리뷰 후속(현황 대시보드는 `upcoming`으로 미래만 보여줘 화면 클러터는 원래 없었음). 격리 store로 stale 1회성·신선·stale 반복 3종 실측(신선만 울림) + 경계값 단위 테스트, 294 통과
- **1.14.8** — 데몬(스케줄러) 일시적 오류로 죽지 않게(무인 24시간 견고성): `wonjang cron run` 루프가 매 틱마다 예약작업·메모리·스킬 저장소를 다시 읽는데, 그 읽기에 `?`가 붙어 있어 **일시적 실패 한 번에 데몬 전체가 종료**됐다 — 그러면 모든 알림(브리핑·공휴일·습관·주간결산·시세)이 **조용히 멈추는데 사용자는 켜져 있다고 믿는** 최악의 무성 실패. 동시 쓰기 중 파일 교체(`atomic_write` rename 창)·`exists()`후 `read` 사이 TOCTOU·순간 I/O 오류로 실제 발생 가능. 루프 내 3곳(`CronStore`/`Memory`/`SkillStore` load)을 **로그 남기고 이번 회차/이 작업만 건너뛰고 계속**하도록 변경(다음 틱에 자연 복구). 시작 단계 로드는 의도대로 즉시 보고·종료 유지. 정적 리뷰로 발견 — 빌드·293 테스트·데몬 기동 생존 확인
- **1.14.7** — 한국어 자연어 별칭 6종 보강(한국어 우선 발견성): 같은 기능을 가리키는 **흔한 한국어 단어로 명령이 안 먹어** LLM으로 새던 것들을 별칭으로 추가 — `가계부`(→지출, 명령 도움말이 *literally* "가계부: 지출을 기록"이라 부름)·`평수`(→평, 도움말이 "평수 변환")·`압축해제`(→압축풀기, 알집·윈도우의 표준 용어)·`이름바꾸기`(→이름변경)·`한글복구`(→깨짐, 기능명이 "한글 깨짐 복구")·`환전`(→환율). 기존 별칭·영어 이름 모두 그대로. [[1.14.5]]의 디데이·약속 별칭 보강에 이은 같은 결의 작업 — 자연어 표현으로 명령을 테스트하다 발견. 6종 신규+6종 회귀 + `가계부 add`→`지출` 동일 저장소 실측, 292 테스트 통과
- **1.14.6** — `변환`(convert) 발견성·안내 개선: ① **stale help 수정** — `convert --help`가 "온도/무게/길이"만 안내해 v0.94에 추가된 **속도·부피·넓이(kmh/mph·l/gal·sqm/sqft)가 보이지 않던 것**을 7개 분류 전부 노출하도록 갱신(기능은 있는데 도움말에서 숨겨져 못 쓰던 발견성 구멍). ② **평 안내** — 한국에서 제일 흔한 변환인 `convert 100 평`이 "몰라요"로만 끝나던 것을, **전용 명령 `wonjang 평 100`으로 정확히 안내**(소수도 보존). 첫 실행 워크스루 중 발견 — 격리 HOME에서 7개 분류·평 안내·소수 입력 실측, 292 테스트 통과
- **1.14.5** — 한국어 명령 별칭 누락 수정(한국어 우선 UX의 구멍): `디데이`·`약속`·`알림`을 **한국어 그대로** 입력하면 명령으로 인식되지 않고 LLM(에이전트)으로 새어 나가 "Not logged in" 같은 엉뚱한 응답이 나오던 것 수정. 현황 대시보드·자랑 카드·도움말은 이 기능들을 **'디데이'·'약속'이라고 부르는데** 정작 그 한국어 단어로는 명령이 안 먹던 불일치였다(습관·지출·집중·할일은 별칭이 있는데 이 둘만 누락). `Dday`에 `디데이`, `Remind`에 `약속`·`알림` 별칭 추가 — 기존 패턴과 통일. 영어 `dday`/`remind`도 그대로 동작. **첫 실행 워크스루를 직접 돌리다 발견** — 격리 HOME에서 `디데이 add`→자랑 카드 D-day 반영, `약속/알림 add`→목록까지 실측
- **1.14.3** — 자랑 카드 좁은 폭 잔디 보존: `--폭 30~33`처럼 좁은 폭에서 28일 잔디가 다 안 들어가면, 앞(오래된 날)이 아니라 **가장 중요한 최신 날짜를 보존**하며 줄인다(이전엔 최신 며칠이 조용히 사라졌음). 폭 30~33 박스 정렬 회귀 테스트 추가. 적대적 리뷰가 남긴 마지막 실제 결함 해소 → 자랑 카드는 이제 폭 30~80 전 구간에서 흠 없음
- **1.14.2** — 선제 알림 중복 방지(데몬 재시작 견고): 공휴일·습관·주간결산·브리핑 알림의 '보낸 날' 표시를 파일(`alert_state.json`)에 원자적 저장 → 데몬이 절전·재실행으로 재시작해도 같은 윈도우에 **중복 푸시하지 않음**. 적대적 리뷰가 짚은 '인메모리 dedup → 재시작 시 중복' 한계 해소. 구버전 파일 호환(누락 필드 기본값), serde 라운드트립 테스트
- **1.14.1** — 대전환 코드 적대적 재검증 후속(13-에이전트 리뷰가 직접 발견·확정한 자기 코드 버그): ① **카드 텍스트표현 기호 폭 오류** — 습관명에 `★♥❤☎`(변이선택자 없는 기호)가 들면 실제 1칸인데 2칸으로 세어 박스 우측 테두리가 어긋나 "카톡에서 안 깨진다"는 계약을 위반하던 것 수정(변이선택자 페어 인식으로 `✍️` 라벨은 2칸 보존). wcwidth 교차검증. ② **`자랑 --달` 한 자리 월** — `--달 2026-6`이 저장 형식(`2026-06`)과 안 맞아 집중 0·지출 오합산되던 것 → 0 채워 정규화. ③ **공휴일 전날 알림 2건** — dedup 플래그를 네트워크 조회 *전에* set해 일시적 실패 시 그날 알림이 영구 누락되던 것 + 연말(12/31)에 다음 해 1/1을 놓치던 것 → 조회 성공 후 표시·`내일.year()` 조회. ④ atomic_write 임시파일 정리(고아 tmp 누수), add()의 next_id `saturating_add`(오버플로 방지)
- **1.12.2** — ID 충돌 방지(데이터 무결성): 모든 저장소(약속·할일·가계부·습관·디데이·즐겨찾기·집중·코인감시·예약작업 9곳)의 `add()`가 새 ID를 **기존 항목 최댓값+1**로 부여하도록 통일. 백업복원·파일 수기편집 후 `next_id`가 어긋나도 ID가 충돌하지 않아, `remove`/`done`이 엉뚱한 항목을 조작하던 위험을 제거. 복원 시나리오(next_id=0·items 1~3 → 새 항목 id=4)로 검증
- **1.12.1** — 일기 분리 버그 + 메일 사전검증 + 세액공제 검증: ① **일기 `## ` 분리** — 일기 본문에 `## 제목`(마크다운 헤딩) 줄이 있으면 가짜 항목으로 쪼개져 기록이 깨져 보이던 것 수정(시각 `YYYY-MM-DD HH:MM` 형식 헤더만 항목 경계로 인식, 회귀 테스트 추가). ② **메일 받는사람 사전검증** — 잘못된 주소(`홍길동 a@b.com` 등)를 첨부 읽기·전송 박스 출력 **전에** 명확히 거부(뒤늦은 실패 방지). ③ **(검증) 실수령 근로소득세액공제 한도가 소득세법 §59와 정확히 일치**함을 확인 — 감사가 의심한 7천만~1.2억 구간은 오탐이라 변경 없음(올바른 코드를 깨지 않음)
- **1.12.0** — 검증 버그 7종 배치(41-에이전트 감사 후속): ① **SMTP 587 STARTTLS** — `WONJANG_SMTP_PORT=587`로 Office365·587전용 사내메일 발송 지원(465 고정 해소). ② **암호화 PDF 가드** — 비밀번호 걸린 PDF를 `pdf합치기`/`pdf회전`/`pdf페이지`가 거르지 않아 합본 시 페이지가 조용히 깨지던 것 → 명확히 거부. ③ **세계시간 빈 도시** — 빈 출발 도시가 조용히 '서울'로 빠지던 것(`"".contains` 항상 true) → 에러. ④ **날씨 구어체 접미사** — '서울시·대전시·세종특별자치시·~도'를 내장 좌표로 인식. ⑤ **이미지 `--배율 NaN`** — 1×1 깨짐 방지. ⑥ **메일 RFC2047 패딩 생략** B-인코딩 제목/보낸이도 디코드. ⑦ **노트 점(.) 든 한글 제목**('2026.05.31')이 .md 안 붙어 사라지던 것 수정. 각각 실측·단위테스트 검증
- **1.11.0** — 데이터 보호(41-에이전트 감사 후속): ① **원자적 저장** — 모든 로컬 저장소(약속·할일·가계부·습관·집중·디데이·즐겨찾기·예약작업·코인감시·또간집·메모리·세션·스킬·설정 14곳)를 임시파일+rename 방식으로 전환. 24시간 데몬과 CLI가 동시에 쓰거나 도중 끊겨도 **파일이 반쯤 쓰여 깨지지 않음**(`util::atomic_write`). ② **CP949 zip 압축풀기 한글 파일명 복구** — 윈도우에서 만든 한국 zip을 풀 때 파일명이 `°è¾à¼­`처럼 깨지던 것 수정(목록 보기와 동일하게 CP949 디코드, zip-slip 방어 유지). 진짜 CP949 zip + 악성 `../` 엔트리로 검증
- **1.8.0** — 안정성 대청소(41개 서브에이전트 종합 감사로 발견·검증): ① **한국어 텍스트 패닉 4곳 수정** — 대화형 LLM 응답·`web_fetch`·`run_shell`·`read_file`가 한글/이모지의 UTF-8 바이트 경계에서 크래시하던 것을 문자 경계 안전 자르기 공용 헬퍼(`util::truncate_bytes`)로 해소(한국어 사용자에게 치명적이던 결함). ② **CP949/EUC-KR CSV 자동 인식** — 한국 엑셀·은행이 저장하는 CP949 CSV를 `엑셀`이 못 읽고 죽던 것 수정 + UTF-8 BOM 제거. ③ **PDF 페이지 범위 OOM 방지** — `pdf페이지 x.pdf 1-999999999`처럼 거대 범위가 펼쳐지며 멈추던 것을 펼치기 전 상한 검증으로 즉시 거부.

## 로컬 파일·비서 작업 (GPT가 못 하는 것)

- **1.23.0** — 가계부를 **CSV로 내보내기 → 분석 연결**: `wonjang 지출 내보내기`로 그동안 쌓인 모든 가계부 기록을 `[날짜·분류·금액·메모]` CSV로. 가계부는 이번 달 분류별 합계만 보여줬는데, 내보내면 **`wonjang 엑셀 가계부.csv --그룹 분류 --열 금액`으로 월별·분류별 피벗·필터·정렬·막대그래프**까지 — 수집(가계부)과 분석(엑셀) 도구가 이어진다. 금액은 원시 숫자로(엑셀 재인식), 메모의 콤마는 정확히 인용(`sheet::to_csv` 재사용). 빈 기록 안내·원자적 저장, 도움·README, 304 통과
- **1.22.0** — 디데이를 **캘린더(.ics)로 내보내기**: `wonjang 디데이 내보내기`(별칭 `디데이 ics`)로 등록한 디데이(수능·기념일·마감 등)를 표준 iCalendar 파일로 — 더블클릭하면 **구글·애플 캘린더에 종일 일정으로** 들어간다. 터미널에만 있던 디데이가 폰 캘린더로 따라와 알림까지. **새 의존성 없음**(.ics는 평문), RFC 5545 종일 일정(DTEND 다음날·UID·DTSTAMP)·텍스트 이스케이프. `ddays::to_ics`(순수)+테스트, **표준 `icalendar` 파서로 출력 검증**(2 이벤트 정상 파싱), 304 통과
- **1.21.0** — **이미지 이어붙이기**(`이미지이어붙이기`, 별칭 `사진이어붙이기`·`이어붙이기`): 여러 이미지를 한 장으로 — `wonjang 이미지이어붙이기 1.png 2.png 3.png --세로`(기본) 또는 `--가로`. 긴 카톡 대화 캡처·영수증 여러 장·비교 이미지를 **하나의 이미지로** 합쳐 공유. 폭·높이가 달라도 캔버스(흰 배경)에 맞춰 합성, **EXIF 방향 적용**(누운 폰 사진도 바로), 투명 PNG는 흰색으로, JPEG로도 저장 가능. 합치면 너무 커지면(>1억 픽셀) 거부하고 `wonjang 이미지`로 줄이기 안내, 원본 덮어쓰기 가드. 실측(세로 200×180·가로 350×100·다른 크기·유효성·별칭) 검증, 303 통과
- **1.20.0** — **여러 표 합치기**(`표합치기`, 별칭 `엑셀합치기`·`csv합치기`): 월별·지점별로 쪼개진 CSV·엑셀을 한 파일로 — `wonjang 표합치기 1월.csv 2월.csv 3월.csv --저장 합본.csv`. 한국 사무의 흔한 잡무(분기 합본·여러 지점 취합)를 GPT 없이 로컬에서. **열 순서가 달라도 머리글 이름으로 맞추고**(뒤 파일에만 있는 열은 추가, 없는 칸은 빈값), `--출처`로 각 행이 어느 파일에서 왔는지 열을 붙임. 합친 결과는 곧장 `엑셀`로 분석(합치기→집계 조합). `sheet::merge_tables`(순수)+테스트(열정렬·출처), 원본 덮어쓰기 가드, 도움·README 노출, 301 통과
- **1.19.0** — 엑셀·CSV 분석 **결과를 새 CSV로 저장**(읽기→분석→내보내기 한 줄): `wonjang 엑셀 회사.xlsx --시트 매출 --필터 지역=서울 --정렬 매출액 --저장 서울매출.csv` 한 줄로 큰 엑셀에서 우리 부서·우리 지역 것만 뽑아 새 파일로 — 한국 사무의 가장 흔한 잡무를 GPT 없이. `--그룹` 집계 결과(분류·합계·평균·건수)도 저장 가능. CSV 인용 규칙 정확(콤마·따옴표 든 셀, 라운드트립 검증), 집계 저장값은 콤마 없는 **원시 숫자**(엑셀에서 숫자로 재인식), **원본 덮어쓰기 가드**. `sheet::to_csv`(순수)+테스트, 원자적 저장, 도움·README·팁 노출, 300 통과
- **1.18.0** — 엑셀 **다중 시트 선택**: 그동안 `엑셀`이 **첫 시트만** 읽어 다중 탭 엑셀(매출/재고/직원 등 한국 사무 파일에 흔함)의 다른 시트를 분석할 수 없던 것을 해소 — `wonjang 엑셀 회사.xlsx --시트 재고`(이름) 또는 `--시트 2`(번호)로 선택. 시트가 여러 개면 헤더에 **시트 목록을 안내**(다른 탭 분석 유도), 없는 시트명은 가능한 시트 목록과 함께 에러. 모든 분석 모드(통계·피벗·정렬·필터·json)가 선택 시트 위에서 동작. `Table::load_sheet`/`list_sheets` 추가, 실측(매출/재고/직원 3시트, 이름·번호·없는시트·집계 조합) 검증, 299 통과
- **1.17.0** — 엑셀·CSV **조건 필터(다른 분석과 조합)**: `wonjang 엑셀 매출.csv --필터 지역=서울` → 조건에 맞는 행만. 핵심은 **조합** — `--필터 매출>1000000 --그룹 지역 --열 매출`처럼 거른 뒤 집계·정렬·통계가 그 부분집합 위에서 동작해, "서울 매출만 지역별로", "100만 이상 거래만 상위순" 같은 즉석 분석을 한 줄로. 연산자 `= != > < >= <= ~`(포함), 지저분한 숫자("1,250,000원") 파싱, 0건이면 명확히 안내. 필터는 `Table::filtered`(순수)로 부분집합 표를 만들어 이후 모든 모드가 그대로 동작 — 단위 테스트(문자열·포함·숫자비교·없는 열·형식오류), `도움`·README·팁 노출, 299 통과
- **1.16.0** — 엑셀·CSV **정렬해서 상위 행 보기**: `wonjang 엑셀 매출.csv --정렬 매출액` → 매출 큰 순으로 상위 N행(기본 5). "어느 거래·지점이 제일 컸나"를 파일 열어보지 않고 바로. 숫자 열은 큰 값부터(기본), `--오름차순`이면 작은 값부터; 텍스트 열은 가나다순. 지저분한 한국 숫자("1,250,000원")도 파싱하고 숫자 아닌 행은 뒤로 보냄. 정렬 로직은 `Table::sorted_rows`(순수)로 분리해 단위 테스트(숫자 내림·오름·텍스트 가나다·비숫자 후순위), 미리보기 팁·`도움`·README에 노출, 298 통과
- **1.15.1** — 엑셀 그룹 집계에 **가로 막대그래프**: `--그룹` 집계 결과를 합계 비율 막대(`█`)로 함께 그려, "어느 분류가 큰지"를 숫자 읽기 전에 한눈에. 보고서·카톡에 캡처해 공유하기 좋은 시각화(전염성↑). `card::hbar`(순수 함수, 항상 정확히 지정 폭칸·max≤0 가드)로 분리해 단위 테스트(비율·클램프·폭 불변식), 297 통과
- **1.15.0** — 엑셀·CSV **분류별 집계(피벗)**: 표 분석에서 제일 흔한 "지점별 매출", "부서별 급여 합계"를 한 줄로 — `wonjang 엑셀 매출.csv --그룹 지점 --열 매출액` → 지점별 합계·평균·건수를 **합계 내림차순**으로. 지저분한 한국 숫자("1,250,000원")도 그대로 파싱, 빈 그룹값은 `(빈값)`으로, 비숫자 셀은 평균에서 제외. GPT가 못 하는 일(**내 로컬 파일을 직접 읽어** 집계)인데 기존 `엑셀`은 단일 열 통계만 가능했음. 평균 표시도 큰 수에 천단위 콤마(`1,616,666.67`)로 개선. 집계 로직은 `Table::group_by`(순수)로 분리해 단위 테스트(합계·정렬·비숫자 제외·빈값), 미리보기 팁·`도움`·README에 노출, 296 테스트 통과
- **0.91.0** — 한글 깨짐 복구: EUC-KR/CP949로 저장돼 깨지는 파일(.csv·.txt)을 UTF-8로 복구(관공서·은행·옛 메모장). 원본 보존(`_utf8` 저장), 이미 UTF-8이면 건드리지 않음, `--되돌리기`로 UTF-8→CP949(옛 시스템 업로드용). encoding_rs (`깨짐`)
- **0.95.0** — 폰 사진 정방향 보정: `이미지`·`사진묶기`가 EXIF 방향(Orientation)을 적용해 옆으로 누운 폰 사진을 바로 세움. 이전엔 픽셀만 읽어 세로 사진이 가로로 처리되던 결함 수정(가장 흔한 입력=폰 사진)
- **1.7.0** — PDF 비밀번호 걸기: `pdf암호 <파일> --비번 <비밀번호>`로 PDF에 **AES-256 암호화**(주민번호·계좌 든 민감 서류를 "비밀번호 건 PDF로 제출" 요구에 대응). 열기 비밀번호 강제 — pypdf로 빈/틀린 비번 거부·맞는 비번만 허용 검증. 원본 보존(`_암호.pdf`). lopdf 공식 방식 (`pdf암호`)
- **0.96.0** — PDF 회전: 옆으로 스캔된 PDF를 90의 배수로 돌림(표준 /Rotate, 페이지 지정 가능). CoreGraphics 렌더로 실제 회전 반영 검증 (`pdf회전`)
- **0.94.0** — PDF 합치기: 여러 PDF를 하나로(서류 합본 제출). lopdf 공식 merge 방식(객체 재번호·페이지 트리 재구성), 메모리상 병합 로직 단위 테스트 (`pdf합치기`)
- **0.93.0** — PDF 페이지 추출: 큰 PDF에서 원하는 페이지(1-3,5)만 새 PDF로(제출·공유). 라이브러리 delete_pages로 페이지 트리 안전 처리, 원본 보존 (`pdf페이지`)
- **0.92.0** — 사진 → PDF 묶기: 여러 JPEG/PNG를 한 PDF로(한 장씩 페이지). 서류 제출·스캔앱 대용. JPEG는 DCTDecode로 임베드(lopdf 구조 작성, 무거운 코덱 없이) (`사진묶기`)
- **1.6.1** — README 갱신(문서): npm/깃허브 첫 화면의 "기능 한눈에"·특징에 이메일(IMAP/SMTP)·PDF(묶기/합치기/추출/회전)·이미지(축소·EXIF·형식변환)·압축 보기·한글 깨짐 복구를 정확히 반영. v0.1 수준이던 설명을 v1.6 제품에 맞춤(신규 사용자 첫인상)
- **1.6.3** — 버그 수정(엑셀 금액 열 `원` 미인식): `엑셀 --열 금액`이 `5,000원`처럼 **원 접미사가 붙은 한국 가계부·은행 CSV 금액을 텍스트로 취급해 합계가 조용히 비던** 문제 수정. `원` 제거 + 회계식 괄호 음수 `(1,000)`→-1000도 인식. 단위 테스트 + 실제 CSV로 검증
- **1.6.2** — 버그 수정(투명 PNG→jpg 검은 배경): `이미지 --형식 jpg`·`사진묶기`에서 투명한 PNG(로고·스크린샷)의 투명 부분이 **검게** 나오던 문제 수정 — 이제 **흰 배경에 합성**. 알파를 그냥 버리지 않고 흰색과 블렌딩(불투명은 그대로). 실제 투명 PNG + PDF 렌더로 검증
- **1.6.0** — 이미지 형식 변환: `이미지 --형식 jpg`/`--형식 png`로 png↔jpg 변환(호환성·용량 — "투명 png를 jpg로", "이 형식 안 올라가서 jpg로"). 리사이즈와 동시 가능, 변환 시 `_변환` 접미사로 원본 보존
- **0.98.0** — 이미지 일괄 처리: `이미지`가 여러 장을 한 번에 줄임(중고거래·블로그·메일에 사진 여러 장 올릴 때). 각 파일 옆에 `_작게`로 저장·원본 보존, 실패는 건너뛰고 요약 (`이미지 *.jpg`)
- **0.90.0** — 이미지 축소·압축: 로컬 사진을 폭/배율로 줄이고 JPEG 품질 조정(카톡·메일 첨부 용량↓). 비율 유지·확대 안 함·원본 보존(`_작게` 저장), JPEG/PNG (`이미지`)
- **0.63.0** — QR 코드 생성: 터미널에 스캔 가능한 QR(URL·와이파이) (`qr`)
- **0.64.2** — 에이전트 시스템 프롬프트 정리(계산기 22줄→요약, `도움`으로 전체 조회). 토큰·집중도 개선
- **0.64.1** — `config`에 서울 K-API 키 상태·무료 키 발급 안내(서울/공공데이터/네이버) 추가
- **0.67.0** — 파일 비교(diff): 두 파일 줄 단위 차이(추가/삭제, similar) (`비교`)
- **0.66.0** — 파일 체크섬: SHA-256/512 계산·무결성 검증(`--확인`), shasum 동일 (`해시`)
- **0.65.0** — JSON 도구: 로컬 JSON 검증·정렬·점경로 값 추출(오류 위치 표시) (`json`)
- **0.64.0** — 파일 내용 검색: 폴더 안 텍스트에서 단어 든 줄 찾기(바이너리 제외, 읽기 전용) (`찾기`)
- **0.68.0** — 엑셀/CSV → JSON 변환: 표를 `[{헤더:값}]` 배열로 출력 (`엑셀 --json`)
- **1.4.0** — 압축 내용 미리보기: zip을 풀지 않고 안의 파일/폴더 목록·용량을 봄(`압축보기`). 윈도우에서 만든 zip의 **CP949로 깨진 한글 파일명을 자동 복구**해서 표시(다른 도구는 깨져 보임). 비zip/손상 파일 거부
- **0.62.0** — 폴더·파일 압축/해제: zip 생성·해제(한글명 보존, zip-slip 방어) (`압축`/`압축풀기`)
- **0.57.0** — 파일 이름 일괄 변경: 파일명 문자 치환(기본 미리보기, `--실행`) (`이름변경`)
- **0.56.0** — 폴더 자동 분류: 종류별 하위 폴더로 이동(기본 미리보기, `--실행`) (`정리`)
- **0.55.0** — 중복 파일 찾기: 내용이 같은 파일·낭비 용량(크기+해시, 읽기 전용) (`중복`)
- **0.54.0** — 디스크 용량 분석: 큰 파일·폴더 찾기(읽기 전용) (`용량`)
- **0.53.0** — 또간집 맛집 찾기: 풍자 〈또간집〉 선정 맛집을 지역으로 검색(직접 키우는 로컬 목록) (`또간집`)
- **0.52.0** — 엑셀·CSV 분석: 로컬 파일을 직접 읽어 요약·미리보기·열 통계(합계/평균/최대/최소) (`엑셀`)

## 생활·금융 계산기 (한국 특화)

- **1.26.0** — 단위변환에 **한국 전통단위**(금은방·정육점에서 매일 쓰는데 GPT가 자주 틀리는 것): `변환`에 **돈**(3.75g, 금·은)·**근**(600g, 고기·채소)·**관**(3.75kg)·**되**(1.8L)·**말**(18L)과 **그램 역환산**(`600g = 160돈 = 1근`)을 추가. "금 5돈 몇 그램?"·"고기 두 근 몇 kg?"는 한국에서 매일 나오는 질문인데 GPT는 근을 600g/375g 사이에서 헷갈린다 — wonjang은 **결정론적으로 정확**하고 기준(고기 600g·금 3.75g)을 함께 표기해 신뢰를 준다. 자연어("금 5돈")도 에이전트가 `변환 5 돈`으로 잇도록 프롬프트·도움말 갱신. 단위 테스트(돈·근·관·되·말·역환산), 실측(금은방·정육점 시나리오), 327 통과
- **0.50.0** — 영문→한글 복원: 한글 조합 오토마타로 잘못 친 글자 복구 (`한타`)
- **0.49.0** — 한글→영문 타자 변환: 두벌식 키 순서 (`영타`)
- **0.75.0** — UUID v4 생성: RFC 4122 무작위 UUID(OS 난수) (`uuid`)
- **0.74.0** — 안전한 비밀번호 생성: OS 난수(getrandom)·종류별 보장·편향 제거 (`비번`)
- **0.79.1** — `지출` 기본 화면에 이번 달 분류별 합계(상위 5) 표시
- **0.79.0** — 간단 일기: 월별 마크다운에 시각과 함께 기록·보기(옵시디언 불필요) (`일기`)
- **0.76.1** — `현황` 대시보드 상단에 오늘 날짜·요일 표시
- **0.76.0** — 색상 변환: HEX↔RGB↔HSL(#rgb 단축형, 디자인·CSS) (`색`)
- **0.73.0** — base64/URL 인코딩·디코딩: JWT·URL 파라미터(한글 지원) (`인코딩`)
- **0.72.0** — 유닉스 타임스탬프 변환: 현재 시각·초/밀리초·날짜↔유닉스(로컬+UTC) (`타임스탬프`)
- **0.48.0** — 진법 변환: 2·8·10·16진수, 0x/0o/0b 접두사 인식 (`진법`)
- **0.47.0** — 시간 계산: 시·분 더하기/빼기, 근무시간 합산 (`시간`)
- **0.46.0** — 사칙연산 계산기: 괄호·소수·음수, 재귀 하향 파서 (`계산`)
- **0.45.0** — `나이`에 띠(십이지)·별자리(12별자리) 추가
- **0.44.0** — 한글 금액 표기: 계약서·수표용 "일금 ...원정" (`금액`)
- **0.43.0** — 한글 초성 추출: 유니코드 분해로 정확 (`초성`)
- **0.42.0** — 글자수 세기: 공백 포함/제외·단어·줄·바이트 (`글자수`)
- **0.41.0** — `도움` 안내에 생활·금융 계산기 14종 그룹 추가(발견성 개선)
- **0.40.0** — 시급·주휴수당 계산: 주급·월급, 주 15시간↑ 주휴·최저임금 경고 (`시급`)
- **0.39.0** — 날짜 계산: 두 날짜 사이 일수·N일 후 날짜·요일 (`날짜`)
- **0.38.0** — 부가세(VAT 10%) 계산: 공급가↔합계 양방향 분리 (`부가세`)
- **0.37.0** — 할인가 계산: 중복 할인(회원+카드) 순차 적용 (`할인`)
- **0.82.0** — 수면 시간 계산: 90분 수면 주기 기반 취침/기상 시각 추천 (`수면`)
- **0.81.0** — 칼로리 계산: 기초대사량(BMR, 미플린-세인트 지어)·활동별 TDEE (`칼로리`)
- **0.36.0** — BMI 계산: 대한비만학회 아시아 기준 판정 (`bmi`)
- **0.83.0** — 로마 숫자 변환: 숫자↔로마자(1~3999, 왕복 검증) (`로마`)
- **0.80.0** — 단위 변환 확장: 속도(km/h↔mph)·부피(L↔gal)·넓이(㎡↔ft²) 추가 (`변환`)
- **0.35.0** — 단위 변환: 온도·무게·길이 (`변환`)
- **0.34.0** — 제비뽑기/추첨: 당첨·순서 정하기 (`뽑기`)
- **0.33.0** — 더치페이(n빵) 정산: 1인당·거스름 (`더치`)
- **0.32.0** — "오늘 뭐 먹지?" 메뉴 추천 (`메뉴`)
- **0.31.0** — 예적금 만기 계산: 세후 이자 (`예금`/`적금`)
- **0.30.0** — 대출 상환 계산: 원리금/원금 균등 (`대출`)
- **0.29.0** — 연봉 실수령액: 4대 보험+소득세 (`실수령`)
- **0.28.0** — 만 나이 계산: 만 나이 통일법 (`나이`)
- **0.27.0** — 평수 변환: 평↔㎡ (`평`)

## 데이터·운영

- **0.26.0** — 데이터 복원(복원 전 자동 백업) (`복원`)
- **0.25.0** — 데이터 백업 (`백업`)

## 실시간 정보 (무료 API)

- **0.61.1** — 아침 브리핑에 "다음 공휴일까지 D-day" 연동(설날·추석 포함)
- **0.61.0** — 서울 따릉이 실시간: 대여소 남은 자전거·거치대(서울 키 슬롯 공유) (`따릉이`, k-skill 참고)
- **0.71.0** — 사이트 상태 확인: HTTP 상태 코드·응답 속도(키 불필요) (`사이트`)
- **0.70.0** — 내 공인 IP: 현재 공인 IP·통신사·위치(ip-api, 키 불필요) (`내아이피`)
- **0.69.0** — GitHub 저장소 정보: 별·이슈·최신 릴리스(공개 API, 키 불필요) (`깃헙`)
- **0.60.0** — 긱뉴스: 개발·기술·스타트업 최신글(GeekNews RSS, 키 불필요) (`긱뉴스`, k-skill 참고)
- **0.78.0** — 시간대 변환: 특정 시각을 도시 간 환산(전날/다음날, DST 정확) (`시차`)
- **0.77.0** — 세계 시간: 주요 도시 현재 시각(chrono-tz, DST 정확) (`세계시간`)
- **0.59.0** — 서울 실시간 혼잡도: 명소·상권의 붐빔 정도·실시간 인구 (`혼잡도`, k-skill 참고)
- **0.58.0** — 한국 공휴일 조회: 설날·추석(음력) 포함, 다음 공휴일 D-day (`공휴일`)

- **0.24.0** — 시세 알림 환율 확장
- **0.23.0** — 코인 시세 알림(목표가 도달 시 푸시) (`감시`)
- **0.20.0** — 뉴스 헤드라인 (`뉴스`)
- **0.19.0** — 코인 시세(업비트) (`코인`)
- **0.18.0** — 실시간 환율 (`환율`)
- **0.17.0** — 미세먼지(대기질) (`미세먼지`)
- **0.16.0** — 실시간 날씨(open-meteo) (`날씨`)
- **0.15.0** — 서울 지하철 실시간 도착 (`지하철`)

## 에이전트 코어

- **0.97.0** — 대화형 원장이 로컬 파일/PDF 능력 인지: 시스템 프롬프트에 `이미지`·`사진묶기`·`pdf합치기`·`pdf페이지`·`pdf회전`·`깨짐`을 안내(자연어 예시 포함). "사진들 PDF로 묶어줘"처럼 채팅으로 바로 시킬 수 있게 — 최근 추가한 CLI 기능을 헤르메스식 대화 경험에 연결(프롬프트 회귀 테스트 추가)
- **0.89.0** — 선제 알림 ②(저녁 습관 리마인드): 24시간 데몬이 저녁(20시 이후) "끊길 위기 습관"(streak≥2·오늘 미완)을 먼저 챙겨 "자기 전에 체크해요" 푸시(하루 1회·푸시 채널 설정 시). streak 가장 센 습관을 대표로
- **0.88.0** — `현황` 상단 "원장 한마디": 로컬 디데이·습관 streak·약속·할 일을 읽어 지금 가장 신경 쓸 하나를 먼저 종합해 말해줌(끊길 위기 습관·임박 디데이 우선). 말할 게 없으면 조용히 생략(비서가 먼저 챙기는 느낌)
- **0.87.0** — 선제 알림(헤르메스식): 24시간 데몬이 공휴일 전날 "내일 빨간날이에요!"를 먼저 푸시(하루 1회·아침 이후·푸시 채널 설정 시)
- **0.86.1** — 도구 실행 표시 한국어화: `weather_now`→`🌤️ 날씨 확인` 등 친근한 라벨·이모지(디버그 로그 → 비서가 일하는 느낌)
- **0.86.0** — 첫 실행 온보딩: 처음 쓰는 사용자에게 한 번, 따뜻한 안내 + 예시 명령 + `/성격` 소개(차가운 온보딩 해소)
- **0.85.1** — 아침 브리핑 개인화: 성격 유지 + 어제 돌아보기(일기·지출·습관 streak) + 내일 공휴일 강조 + 미세먼지 경고("선톡"처럼 먼저 챙김)
- **0.85.0** — 첫인상 배너 재설계: 실제 버전·"🔑 키 없이 연결됨" 강조·성격별 인사·오늘 날짜(첫 5초 wow)
- **0.84.0** — 에이전트 페르소나: 말투·태도를 고르는 `성격`(다정·친구·집사·선배·발랄), 사용자 소유 SOUL.md를 시스템 프롬프트 맨 앞에 주입(헤르메스 방식) (`성격`/`/성격`)

## 개인 비서 기능

- **1.25.0** — **슬랙 푸시 알림** 추가(한국 회사에서 흔한 채널): 기존 디스코드·텔레그램·카카오에 더해 **슬랙 Incoming Webhook**으로도 푸시. `WONJANG_SLACK_WEBHOOK`에 웹훅 URL만 넣으면, `notify`·아침 브리핑·주간 결산·시세 알림·약속 알림이 슬랙으로도 간다. 24시간 데몬·`notify` 명령 모두 자동 연동(`push::slack_send`, POST `{{"text": ...}}`). 채널 감지·설정 안내·도움말·README·에이전트 프롬프트 갱신, 단위 테스트. 순수 rustls(빌드정책 준수, 새 의존성 0)
- **1.5.0** — 메일 검색: `메일검색 <키워드>`로 보낸이·제목에서 메일 찾기("영수증 메일 찾아줘"). 서버 검색의 한글 불안정성을 피해 최근 N통을 받아 클라이언트 측 필터(한글 완벽, 대소문자 무시). 결과 번호가 `메일읽기 <번호>`와 그대로 연동. 이메일 목록·본문·검색·첨부·발송 한 묶음 완성
- **1.2.0** — 메일 첨부파일 저장: `메일첨부 <번호> --저장폴더 <경로>`로 그 메일의 첨부를 디스크에 저장(계약서·명세서·청구서 PDF가 메일로 올 때). MIME 파트에서 첨부 추출(base64/QP 디코드), 파일명 RFC2047 디코드 + 경로 탈출 방어, 충돌 시 (n) 자동 부여. 저장 후 `엑셀`/`pdf페이지` 등으로 바로 이어 처리. 샘플 .eml 단위 테스트
- **1.3.0** — 메일 첨부 발송: `메일보내기`에 `--첨부 <파일>`(여러 번 지정 가능) 추가 — "이 PDF 메일로 보내줘". multipart/mixed 구성(확장자로 MIME 추정), 보내기 전 첨부 파일명·용량까지 echo. 메일 받기↔보내기가 첨부까지 대칭 완성. lettre Message를 .formatted()로 오프라인 단위 테스트
- **1.1.0** — 메일 보내기(SMTP over rustls): `메일보내기 --받는사람 --제목 --내용`으로 메일 발송(lettre, openssl 없이 순수 rustls). 보내기 전 받는이·제목·내용을 echo로 명확히 표시(외부 전송은 되돌릴 수 없으므로). 도메인으로 SMTP 호스트 자동 추정. 에이전트는 전송 전 사용자 동의 필수. 이메일이 읽기+쓰기로 완성
- **1.0.0** — 🎉 첫 정식 릴리스 + 메일 본문 읽기: `메일읽기 <번호>`(1=최신)로 특정 메일 본문을 읽음(MIME 멀티파트 파싱, text/plain 우선·HTML 태그 제거, 한글 charset/전송인코딩 디코드). `메일` 목록에 번호 표시로 바로 열기. 이메일이 목록→본문까지 완성되며 개인 비서 핵심이 갖춰짐(mailparse, 순수 Rust). 샘플 .eml 단위 테스트
- **0.99.0** — 이메일 받은편지함 읽기(원래 #1 니즈): IMAP over rustls로 실제 메일함 접속(`메일`, `메일 --안읽음`). 자격증명은 환경변수(WONJANG_EMAIL·WONJANG_EMAIL_PASSWORD 앱 비밀번호)로만 — 설정 파일에 비밀번호 저장 안 함. 미설정이면 친절한 설정 안내. 한글 제목 RFC2047 디코드(UTF-8·EUC-KR), 도메인으로 IMAP 호스트 자동 추정. openssl 없이 순수 rustls. 대화로도 "메일 왔어?"
- **0.22.0** — 도움 안내
- **0.21.0** — 로또 자동번호 (`로또`)
- **0.14.0** — 즐겨찾기 + 빠른 열기 (`즐겨찾기`/`열기`)
- **0.13.0** — 자동 아침 브리핑
- **0.12.0** — 현황 대시보드 강화 + 회고
- **0.11.0** — 집중 타이머(뽀모도로) (`집중`)
- **0.10.0** — 습관 트래커(streak) (`습관`)
- **0.9.0** — 가계부(지출 관리) (`지출`)
- **0.8.0** — 카카오톡 푸시
- **0.7.0** — 노션 연동 (`notion`)
- **0.6.0** — 현황 대시보드 (`현황`)
- **0.5.0** — 디데이(D-day) 관리 (`dday`)
- **0.4.0** — 클립보드 연동(번역·요약·저장)
- **0.3.0** — 푸시 알림(디스코드/텔레그램) (`notify`)
- **0.2.0** — 한국 개인비서 기능(옵시디언·약속/알림·반복·할일·브리핑)

## 시작

- **0.1.0** — 첫 릴리스: 멀티플랫폼 빌드 + npm 배포, Claude Code/Codex 백엔드 연결

[모든 릴리스 보기](https://github.com/wonjangcloud9/wonjangAgent/releases)
